# The lab — the disposable machine

The guard (the `omarchy-firmware` tool, the repo root) lives under one
founding rule: **+0 octet — the running machine's firmware is never
written, and its deeper layers are only read through an explicit
`--spi-read` gesture**. The lab exists because some questions can only
be answered by *replacing* firmware — and those questions are answered
here, on machines that exist to be broken.

## The doctrine (three sentences)

1. The lab is **disposable by construction**: a VM first (QEMU + OVMF),
   a sacrificial board later — never the day-0 machine, never a machine
   anyone depends on.
2. Everything learned here is **transferable knowledge**, not a fork of
   the guard: the lab informs the study (volumes 4 and 5), it does not
   change the tool's contract, its tiers, or its +0 octet rule.
3. **Replacing the original firmware is legitimate in the lab** — the
   original image is dumped, hashed and archived BEFORE any write, and
   the write itself goes through an external programmer on real
   hardware. A lab without a backup is just a brick factory.

## What lives here

### The findings (chronological, Markdown, the narrative)

| File | What it is |
|---|---|
| `ovmf-findings.md` | the first real firmware the parser has read: the LZMA compression wall pierced in pure stdlib (0 visible modules ≠ empty firmware → full census restored), the 4 MiB / 16 MiB geometry |
| `findings-first-ring.md` | the massive investigation, 7 fronts: species census, ROM chains, the physical trust chain, the PSP lens |
| `findings-second-ring.md` | 7 fronts: the NVRAM journal walked byte by byte, the boot chain resolved by position, the dependency web, the security x-ray |
| `findings-third-ring.md` | 7 fronts: the NX policy is three bytes (hash-diff or nothing), the first instruction is a CR0 fork, the facade quantified (~423 sentences), the NVRAM priced |
| `findings-fourth-ring.md` | 7 fronts: the gates x-ray, the PE provenance (zero timestamps, no PDB), the entropy atlas (58.9 % void), the SMM anatomy |
| `findings-fifth-ring.md` | 3 fronts: enrollment is data (secboot == ms == snakeoil, byte-identical), the facade package layer anchored, ACPI is templates not tables |
| `findings-sixth-ring.md` | 2 fronts: the IFR grammar re-derived spec-fresh (FORMS = 0x02, `{OpCode:8, Length:7, Scope:1}` — why ring 5 locked), the facade counted in QUESTIONS (146 plain / 208 secboot, 21/21 packages close exactly) |
| `findings-seventh-ring.md` | 4 fronts: the S2 lock was a category error (length-prefixed blobs, no package list ever ships — HiiAddPackages builds it at runtime), the SIBT layer decoded, the facade rendered in words (en-US + fr-FR), the facade's memory (varstores named, offsets, defaults, 48/146 questions behind a condition) |
| `findings-eighth-ring.md` | 3 fronts: the dispatch graph (DEPEX as a DAG — zero BEFORE/AFTER, hubs by fan-in, apriori declared order, gate delta empty where modules are shared), the string closure (SCSU = zero real blocks, SKIP2 lives only in fr-FR, the last ids are the null marker + collisions), and pkg3 dissolved (a spurious exact-consumption anchor nested inside the real package — the flat 208 was 202 + 6 double-counted patterns) |
| `findings-ninth-ring.md` | 3 fronts + the owed render check: the GUID nameplate (618/618 occurrences across 13 artifacts named, 209 distinct GUIDs, zero raw cells left — the 245-B formset was the File Explorer formset, the keyring owner is EnrollDefaultKeys' own FILE_GUID), the signature-list lens (PK/KEK/db/dbx record-by-record, the dbx = sha256("") byte-exact in both enrolled stores, 1000-entry synthetic dbx parsed in 2.3 ms), and the weight map (DXEFV 31→41.5% used, secboot grows +87 KB inside the outer slack, the swap = +18 SMM modules − Shell and its dynamic commands, net +810 KB) |
| `findings-tenth-ring.md` | the general rehearsal: the 14-stage pipeline chained end-to-end for the first time (run 1 = 14/14 exit 0 in 25.4 s but 3 artifacts byte-changed — verdict PARTIAL), the nondeterminism classified into three species (set-iteration order in ms-delta, artifact/producer staleness in scsu — the regenerated superset adopted, wall-clock noise in siglist — registered, not pretended stable), the facade's mid-chain churn recorded as converging data, run 2 = REPRODUCIBLE (16/16 end-identical, zero restored), and the day-0 vendor-image protocol fixed as a repo document |
| `findings-eleventh-ring.md` | the instrument meets real vendor silicon: two AMI-Aptio AM4 specimens acquired at the CDN (MSI B450 TOMAHAWK MAX 7C02v3G1 — the frozen 2023 cliff; Gigabyte B450 AORUS PRO F67c — the living line, Aug 2026, CVE-2026-6726/6727 fixed), the nesting rule bites its own registry (case-sensitive FS-GUID lookup rejected 24/24 valid FVs — fixed, both bug and catch registered), the OVMF LZMA grammar crosses to vendor silicon (453/498 modules pierced, 77.5/84.7 % UI-named, MSI's AGESA 1.2.0.8 byte-verified), the same bones measured (275 shared module GUIDs, jaccard 0.549), and the dispatch layer reads 100 % (228/228 + 278/278 DEPEX parsed, PcdProtocol hub fan-in 202/270 vs OVMF's 56) |
| `findings-twelfth-ring.md` | the quorum: the blocked files find their way (ASRock's 403 passes to a full browser header set; ASUS's official GetPDBIOS API names the canonical CDN URL — 4655 and 3604 acquired as CAPs), the latest-official ledger verified per vendor and the ring-11 3644 claim corrected visibly (no such BIOS in the official ASUS ledger — the metadata was Gigabyte F67c's), the 2,048-byte CAP wrapper proven by first-FV delta, the same instruments imported verbatim on two new boards (ASRock 633 modules 60.3 % named, DEPEX 253/253; ASUS 600 modules, DEPEX 286/286), the packaging lens (ASUS: 384 VERSION sections, ONE UI — the nameplate goes structurally blind), and the quorum matrix (living lines cluster 0.70–0.78, the frozen 2023 MSI is the outlier 0.54–0.58, 41–43 GUIDs shared with OVMF each) |
| `findings-thirteenth-ring.md` | the two lenses: the VERSION-strings lens refutes itself (1,992 sections, 100 % PI-spec str@2, but 8 distinct strings — build numbers, not names; the ASUS blindness is structural), the names recovered anyway by the GUID join (665-GUID map, zero real conflicts, ASUS 0.002 → 0.593 — DXE 0.83 / SMM 0.86; the UefiRaid case: the one real version string, '1.0' → '9.3.0.00308', catches ASUS up with the quorum), and the PSP lens (74/74 directories parse, fletcher32-validate and cross-check against psptool 3.6 on all five specimens; $BSP zero closed by source — the real magics are $BHD/$BL2; the PSP layer churns 38/75 blobs between ASUS releases while packaging stands still; PspFtpmHandler in every board) |
| `findings-fourteenth-ring.md` | the NVRAM opens and the register renames itself: the ring-11 "EVSA" premise dies by scan (EVSA=0, $VSS=0 on all six — the format is AMI NVAR), the grammar imported from UEFITool's Kaitai spec file-by-file (never from memory), the store anatomy proven by the spec's own variables (PlatformLang/Timeout resolve to gEfiGlobalVariableGuid on every board after the GUID-area direction fix — caught before publication), the factory grammar across four vendors (one outer StdDefaults variable whose DATA is a nested NVAR store; 9 shared names + per-vendor tails), the MSI mirror is not an NVRAM clone either (Setup 1,428 B vs 1,972 B), the 3644 question narrows (the ring-11 zip = a 240-byte HTML page; 3644 absent from THREE official ledgers; TUF 4645 acquired — the first vendor-sha256-certified specimen), and the reproducibility ritual retires the ring-13 "one run per lens" caveat (NVAR 2 runs identical; PSP re-run identical) |
| `findings-fifteenth-ring.md` | what an update changes, and the chain that holds: the release-delta lens diffs PRIME B450-PLUS 3604 (2022) vs 4655 (2026) file-by-file at body-hash granularity (616 GUID set: 285 unchanged / 322 changed / 7 added / 2 removed — 46.3 % of the board byte-identical across 4.5 years; the churn is the AGESA family — AmdApcb*/AmdCcx*/AmdCpm* — with AmdCcxZen3Dxe growing 71,958 → 85,942 B; the 7 ADDED modules are ASUS flash hardening: PrepareWhiteListSmm, SbRomArmorSmm, FlashSmiSmm, FlashSmiDxe; the ring-14 NVRAM anchor re-derived independently — same 130,952 B size, different content), and the second generale: the six vendor lenses chained in protocol order (unwrap → census → versions → PSP → NVAR → depex), run 1 = COMPLETE-NOT-IDENTICAL, the ritual catches the SAME species ring 10 caught (set-iteration key order in phases_delta — one `sorted()` at the source), run 2 = REPRODUCIBLE (6 stages × 2 runs, all registers byte-identical, ~8 s per chain — the day-0 vendor table is fixed) |
| `findings-sixteenth-ring.md` | the levels, the waves, the nine bytes: byte forensics on the same-size pairs (AcpiTableDxe changed by exactly 9 bytes in 8 spans of 1-2 B inside cmp/jcc sequences — a surgical patch; ACPI rewrote 58.5 % of itself at constant size; the NVRAM anchor's 778-B diff lands exactly in the HWM/QFan default region — NV_SIO0_LD1, SetupHWMOneof, QFan — the factory fan defaults moved; and the "one-byte change" revealed as a wholesale payload replacement sharing 0 % of its overlap — body-hash identity vindicated by its own edge case), the pierced AGESA lens closes all four 'unverified' claims (ASUS 4655 = 1.2.0.12, 3604 = 1.2.0.6b, TUF 4645 = 1.2.0.F — NEWLY VERIFIED; MSI/Gigabyte/ASRock MATCH — zero vendor lies; the strings sit 2.7-2.9 MiB into the decompressed 0x9c0000 FV where the raw scan could never look), and the flash-armor bisect over the official ledger (40 entries now, was 39): the armor arrived in TWO WAVES — core SMM set by 4202 (2023-08-02, first verified carrier, bracketing the LogoFail year) and complete at 4604 (2024-04-08); one legacy SMM module retired in stages (1/2 at 4202, 0/2 by 4402); day-0 gets a versioned armor checklist and a third independent dating signal |
| `findings-seventeenth-ring.md` | the mirror was never a mirror: the MSI double structure root-caused — the 32-MiB file is TWO complete 16-MiB SPI windows (every structure pairs at +0x1000000 exactly: small FVs, main FVs, data FVs, NVRAM anchors, PSP chains), carrying DIFFERENT builds of the same release (380 shared GUIDs: 261 identical / 119 changed; 69 lower-only, 85 upper-only) split by CPU family — lower = Zen/Zen+ (Raven/Pinnacle/Summit), upper = Zen2/Zen3 (Matisse/Renoir/Cezanne, `AmdCcxZen3Dxe`, `SmuV12Dxe`); the `2PSP` mystery opens into AMD's documented combo architecture (PSP2/BHD2 level-2 directories, doc #55758 fetched via coreboot's combo_directory.h — the cookie is the dword 0x50535032): three fletcher-valid tables route 5 x `$PSP` and 2 x `$BHD` per chip ID, re-assembling ring 13's 18 directories without a remainder and dissolving every inherited mystery (+16 files = more families, not a clone; the two Setup defaults 1,428 vs 1,972 B = each window ships its own setup database, the upper with 195 KB more); the 84 shared same-size pairs span 42,441 bytes in 3,081 spans — the same species ring 16 measured; and the ring-8 DriverHealthManagerDxe curiosity closes as INTENTIONAL formset routing (BdsDxe + UiApp carry the configure formset GUID in code-adjacent rodata, stable across three builds — bytes + context + stability decide) |
| `findings-eighteenth-ring.md` | the wave was never a reaction: the three withheld downloads spent (3802, 3810, 4003 probed with the ring-16 machinery, register after every probe) — the core SMM armor first measures 5/7 at 3802 (2022-05-12), the release IMMEDIATELY following 3604 in the ledger, so the wave-1 boundary is exact at official-release granularity; the ring-16 security reading is corrected by measurement (the armor predates the LogoFail disclosure by ~14 months — what lands inside the disclosure window is the legacy-SMM RETIREMENT: 2/2 at 4003, 1/2 at 4202, 0/2 by 4402); the method lesson registered (a binary search answers the question it was asked — 'present by' was true and misleading at once); and the pierced AGESA lens becomes a nine-point dated chronology on one board (3604=1.2.0.6b → 4655=1.2.0.12, monotone, zero new downloads) |
| `findings-nineteenth-ring.md` | the anatomy of the vendor line: seven fronts on the 13 on-disk specimens, zero new acquisitions — PE provenance across 5,707 modules (zero PDB, four timestamps = two fossil MSI blobs of 2012/2014 mirrored in both windows, the NX badge inverted: vendor arms 0/5707 vs OVMF's handful), the raw-image geometry priced for day-0 (ASUS skeleton 56-60 % packed / ~1.06 MiB slack, MSI 34 % erased), trust material inverted (zero EFI_SIGNATURE_LIST in 13 images; raw Authenticode DER chains, ONLY ASUS: 12→18 certs, leaf = TW 23638777 = ASUS, timestamp chain 2021→2025 = a fourth dating signal), the PSP churn table (42/50 types move at 3802 = the platform event cross-confirmed; 7/50 at 3810 = the quiet release), the armor opened (SbRomArmorSmm ONE body across 4.5 years, PrepareWhiteListSmm carries 'AMD rom armor' + an SPI chip database), the CVE patch lens (Gigabyte F65→F67c rebuilds 81.9 % of the board in 4 months — a security patch is a platform rebuild), and the vendor facade quantified (6,360-8,646 questions per board, 100 % valid walks, ~zero IFR conditions — hiding is variable-level, not question-level) |
| `findings-twentieth-ring.md` | the clocks, the quorum, the unasked bytes, and the rebuild: four fronts after the corpus was RESTORED (sandbox reset wiped the binaries; five specimens re-pulled from the vendor CDNs, zip + ROM sha256 verified byte-exact against the ring-11/12 register; the ASUS CDN 403 falls to a full browser header set with Referer) — the five dating clocks FUSED (the 3802|3810 quiet pair — shared AGESA, shared 5/7 armor, shared 211 PSP blobs — is separated by exactly ONE state clock: the DER cert set 12→18 at 3810; the minimal resolving pair is (cert_count, AGESA), and the day-0 decision tree is now cost-ordered), the SMM quorum across four vendors (the four named armor modules ship under the SAME GUIDs on MSI/Gigabyte/ASRock — the armor is AMI-generic, not ASUS-made; SbRomArmorSmm has 3 bodies with MSI=ASRock byte-identical; MSI still carries the legacy SMM ASUS retired at 4202; 83-GUID all-5 spine; a per-occurrence counting bug fabricated a false all-5 armor spine and was caught by the variants probe before publication), the unasked bytes (84-90 % of varstore bytes are never referenced by any question; the Setup blob carries 142-567 never-asked bytes; IFR-side Setup sizes cross-validate ring 14's NVRAM side — the Q1↔Q5 bridge lands from both ends; gates reproduce ring 19's question totals EXACTLY on all five), and the MSI mirror at the PSP layer (the upper window's PSP tables parsed for the first time — 10 tables mirroring the lower's extras at +0x1000000 with same counts/sizes but REBUILT bodies: 72/76 paired blob keys changed, 3.7 % byte-stability vs the FFS layer's majority-identical — the mirror is a parallel build, not a copy) |
| `findings-twenty-first-ring.md` | the sixth clock, the hidden map, and the inverse boundary: three fronts on the RESTORED nine-rung ladder (the seven deleted rungs re-acquired zip-sha256-gated; the ring-16/18 register's rom_sha256_16 entries proved DEAD METADATA — 0/7 reproducible from byte-verified zips across 8 wrapper variants, probe source erased by the sandbox reset — superseded in place, never erased, and the restored binaries reproduce the ring-18 armor census 9/9 before any reading) — the microcode clock (psptool 0x66 lens: exactly 19 patches per specimen — the carrier constant — newest-patch date MONOTONE across the ladder 2021-07→2024-08, moving at 5/8 rungs; the quiet pair 3802|3810 NOT separated — certs remain the only splitter; (cert_count, AGESA) stays THE minimal resolving pair; cross-vendor coherence: MSI 2023-03 freezes at the 2022-03 microcode era, Gigabyte 2026 and ASRock 2025 both at 2024-08-22 — AMD stopped refreshing AM4 microcode at the 4631 generation; my own probe's date bug — max() over DD/MM/YYYY — caught and registered), the hidden map (ring-20 machinery verbatim, gates 5/5 exact: the never-asked Setup offsets as merged ranges — ASUS 456 B/142 B/27 ranges IDENTICAL at 3604 and 4655: the Setup layout frozen across 4.5 years; MSI hides the most, 567 B in 166 fragmented ranges; knowledge-only, zero-write untouched), and the ACPI lens (checksum-admitted tables: the vendor ships the AML ONLY — 4 distinct DSDT variants + 18-23 SSDTs per board — and BUILDS every static table: ZERO FACP/APIC/MCFG/HPET/IVRS bytes in any image, the signatures exist only as generator vocabulary inside the AMI ACPI PEs — the Q8 boundary is the INVERSE of OVMF's; the first gate imported the OVMF expectation, was refuted at byte level by the debug probe, and was rewritten; the DSDT clock: three body generations moving at 4202→4402 — where the DSDT SHRANK 2,141 B, the same release the legacy SMM fully retired — and 4604→4631; compiler INTL 2014-09-25 never moves; no table body identical across all twelve boards) |
| `findings-twenty-second-ring.md` | the eighth clock, the collapsed census, and the frozen whitelist: three fronts on the re-restored corpus (13/13 zips re-gated on register anchors, 12/12 ROMs on ring-21's corrected sha256, zero new acquisitions) — the SSDT clock (the SSDT SET is the eighth clock with frontiers DISJOINT from the DSDT's: one AOD table rev 15→153 swaps at the armor release 3802, the CPM family rewrites 15 bodies wholesale at AGESA 1.2.0.8's 4003; the ring-21 RAW-SEC census reconciled 4+23 plus 8 EMBEDDED ALIB bodies; 74 ALIB bodies corpus-wide, 6 byte-identical across all 13 boards — AMD's common AML layer), the DER inventory (the 12/18 "certs" collapse to SIGHTINGS — the factory material is 4→6 DISTINCT certs gated 9/9, the count freezing at 3810 while the BODIES rotate: DigiCert timestamp chain re-issues at five frontiers 2021→2025, ASUS leaf renews at 4631; PRIME 4655 = TUF 4645 identically; negative serials registered on three vendors' certs), and the armor chip database (46 SPI chip families byte-identical across five vendors, FROZEN 3802→4655; 3604 carries the 41-family predecessor — the whitelist belongs to the AMI flash driver, predates the armor, grew +5 at it, then never learned again) |
| `findings-twenty-third-ring.md` | the churn atlas, the blob authorship, and the speaking ladder: three never-asked fronts on the owned corpus, zero downloads — the CHURN ATLAS (every FFS body hashed along the nine rungs: the armor's birth certificate becomes MODULE-EXACT — wave-1 is exactly five files born at 3802 joining the ring-20 quorum GUIDs one-to-one (FlashSmiDxe/FlashSmiSmm/PrepareWhiteListSmm/SbRomArmorSmm + freeform 89BE47F4), wave-2 exactly two at 4604 (02076249, 4EB43107); the legacy SMM retirement is exactly two smm_driver deaths (827E45A4 at 4202, 21782819 at 4402); the quiet pair is the ladder's quietest frontier at FFS granularity too — 0 born/dead, 45 moved vs 85-238 for neighbors; TUF 4645 vs PRIME 4655: 613/613 GUIDs shared, 473 bodies identical, 0 uniques), the PSP BLOB AUTHORSHIP (2,900+ directory entries admitted by an independent fletcher32 validator reproducing ring 15's MSI 18/18 crosscheck and ring 20's 10-table upper mirror — and the answer: only 3 blob bodies are universal across the four vendors (one 4-KB body shipped twice via $PSP|0x22 and $PL2|0x22, one 160-KB $BL2|0x63); 105 keys are shared by 12 of 13 — everything EXCEPT MSI, the PSP-layer outlier like every other layer; 26 keys only by asus-4655+gigabyte, the newest lineage; the registered "211 shared PSP blobs" declared DEAD METADATA under all five admissible scopes — 133/112/145/201/186 — the ring-18 probe's counting scope died with its sandbox reset, superseded in place), and the VOCABULARY CLOCK (the string layer decoded along the ladder: 27 modules / 29 packages / ~5K distinct strings per rung — the security waves are WORDLESS: zero new words at 4003, 4202, 4604; the ONE security-flavored option the facade ever gained is `PSP RPMC Switch`, born exactly at the armor release 3802, the vendor's own help text stamping it "for test purpose only, NOT FOR PRODUCTION!!!"; +32 storage-template strings at 3810; PCIe bifurcation rewording at 4402; Stability Boost at 4631; Q-Fan renaming at 4655; the body clock and the word clock are disjoint instruments — 224 bodies moved at 3810→4003 with ZERO words) |
| `findings-twenty-fourth-ring.md` | Volume 5 rehearses in software: coreboot 25.12 built for QEMU q35 through three root-less toolchain walls (iasl + the cached xcompile that ignores PATH fixes, 32-bit libgcc for the unwrapped __udivmoddi4, the -print-libgcc-file-name lie under -m32 fixed by a wrapper), the first before/after photograph (vendor PI/FFS2 vs coreboot CBFS — zero shared container), the flash cycle rehearsed file-level 5/5 with the failed-verify branch caught, and the candidate matrix verified against the 25.12 tree (framework/azalea = Framework 13 AMD 7040 — the PSP lens transfers; T440p AND the X470D4U lane absent from 25.12; B450-PLUS control-only) |
| `findings-twenty-fifth-ring.md` | the lens is code, the wall falls, the boot speaks: the CBFS lens written as a standalone instrument (constants imported at runtime from the 25.12 tree's own headers, FMAP candidates validated by semantics — three `__FMAP__` string literals would have hijacked a naive scan; 13/13 identical vs cbfstool AND vs the ring-24 photograph; the image self-describing: defconfig + build stamp `cc0358747d2a-dirty` + payload segment table decoded), the "no QEMU without root" wall fallen by the ring-24 deb-extraction playbook (96-pkg Depends closure, 28 extracted, ldd clean, QEMU 10.0.11), and the FIRST BOOT of a firmware built in this study (exit 0, TCG, 73 s: bootblock→romstage→postcar→ramstage→SeaBIOS 1.17.0→the expected no-bootable-device branch) — the runtime fetching exactly what the static census measured, 4/4 offsets and sizes + mcache 13/13 |
| `findings-twenty-sixth-ring.md` | the chain speaks, and the chain is timed: a bootable disk manufactured from nothing, root-less (byte-by-byte cpio-newc initramfs, hand-built objcopy UKI — the Omarchy boot model; the diagnostic trail kept: syslinux 6.04 and GRUB 2.12 both reject a valid FAT behind a hand-written MBR partition while the superfloppy at LBA 0 mounts everywhere — the failure class named and worked around) ; the full chain on BOTH worlds with the kernel self-qualifying its firmware mode from inside Linux (bios via SeaBIOS+syslinux, uefi via OVMF fw_cfg, 3/3 each) ; and the study's first measured A/B (same q35/TCG/512 MiB/kernel: kernel start 4.57 s vs 3.46 s, userspace 7.49 s vs 6.46 s, the kernel stage invariant at ~3 s — the whole Δ is the firmware stage) |
| `findings-twenty-seventh-ring.md` | the silence decomposed, and our own stub speaks: the ring-26 open thread closed by MEASUREMENT, not earlyprintk — RC1 the UKI sections are inert on the x86 stub (arm C speaks+panics with the initramfs still embedded in its own PE: systemd-stub's food, never consumed), RC2 the decompression knife-edge (`efi_random_alloc` for max(output_len, kernel_total_size); the misleading "Failed to decompress kernel" = the EFI_OUT_OF_RESOURCES allocation failure before any decompressor runs; fw_cfg floor measured 128✗/192✓ → alloc_size ∈ (~55, ~120] MiB; at 512 MiB the UKI's 13.2 MiB pre-load flips the disk path 5/5 to fail while the pristine exec speaks), RC3 empty LoadOptions = a mute boot (the earlyprintk front closes with its true answer: delivering the cmdline is the whole game) — plus two registered consumer contracts (bcfg optional-data rejected by this OVMF shell; the 6.12 LoadFile2 size probe MUST return EFI_BUFFER_TOO_SMALL) — and FRONT 27B: FW27, the study's OWN EFI stub (~260 lines C on gnu-efi 3.0.18 extracted root-less, LoadFile2 defined from UEFI 2.10 §13.6, sections consumed by OUR code, LoadFile2 installed on LINUX_EFI_INITRD_MEDIA_GUID, pristine \boot\vmlinuz LoadImage'd from the same volume, cmdline CHAR16-widened — the v1 CHAR8 mojibake kept as history), full trail on the wire and CHAIN OK 3/3 at 512 MiB with NO shell, NO boot entry, NO fw_cfg, and a memory-map summary the mute vendor firmware would never print |
| `findings-twenty-eighth-ring.md` | the world-naming stub, and the second world: FW28 = FW27 + identity (gST->FirmwareVendor, every config-table GUID dumped with a 5-byte anchor, SMBIOS2/3 entry points parsed byte-explicitly, type 0/1 walked to type 127, verdict with an honest unknown) — and the study's first DUAL-WORLD MATRIX: the same disk, the same stub, the same kernel under OVMF (3/3 green, verdict `unknown world` — Debian masks the upstream identity, the honesty contract fires as designed) AND under coreboot 25.12 + EDK2 UefiPayloadPkg edk2-stable202608 (3/3 green, verdict `coreboot world (edk2 payload above it)` — SMBIOS type 0 `bios-vendor: "coreboot"` handed coreboot→HOBs→SmbiosDxe→config table→our stub); the kernel names OUR LoadFile2 instance as the initrd source in both worlds; six walls named on the way (parent-repo git poisoning the version step, the restored-.git submodule gate vs UPDATED_SUBMODULES=1, the prefixed-compiler libgcc lie under -m32, the SMMSTORE→no-FVB→arch-protocol cascade ending at DxeMain.c:578 with the EMU lane as the principled q35 answer (QEMU flash is ROM under -bios), the stale EDK2_SERIAL_SUPPORT negation that muted TerminalDxe, and the sandbox reaper that kills every background process at the call boundary) |
| `findings-twenty-ninth-ring.md` | the RELEASE lane, the interrupt storm, and the vestigial abort: front 29b closes ring 28's stack-RO sub-question MEASURED, not guessed — 1806 events of vector 0x40 and exactly one #PF on the `-d int` wire; the last five v=40 land at the SAME IP (the `ret` of AsmEnableInterrupts, CpuDxe+0x1021) with SP descending exactly 0x660 each; the fatal `fxsave (%rdi)` (CpuDxe+0xDCAC) faults e=0003 against a STALE RO translation while the gdb post-mortem shows the region's PDE merged back to 2-MiB RW — the verdict: an un-EOI'd level-triggered HPET cascade (HpetTimerDxe, `PcdHpetLocalApicVector`=0x40, IOAPIC level routing) descending the DxeCore stack to the 0x4e00000 boundary, NOT a static guard page; front 29a takes the lane vendor-faithful (CONFIG_EDK2_RELEASE=y, same disk/stub/kernel): 5/6 boots green with the stub speaking ZERO-DEBUG, 1/6 the same storm signature — ring 28's DEBUG 3/3 was partly kind fiction; front 29c source-closes the AcpiPlatform abort (the OVMF-heritage FV-table installer hunting a storage file the payload FV never carries — EFI_ABORTED benign-by-design while coreboot→HOBs→AcpiTableDxe does the work) |
| `findings-thirty-first-ring.md` | the storm, caught live: the two ring-30 residuals closed by measurement — front 31a the storming boot under PDE watchpoints (22 watched boots, 2 storms, moat-write sequences byte-identical to green, fatal CR2s always just below 0x4e00000); front 31b the SMMSTORE lane rebuilt and its DETERMINISTIC storm dissected (reproduction byte-identical to ring 29: RIP 0x1F970CAC, CR2 0x4DFFF60; 64 deliveries observed live with the 0x660 nesting step visible) — and THE CORRECTION: 0x4c000e1 decodes P+A+D+PS with RW=0, ring 29's post-mortem PDE was STILL READ-ONLY, the merge-back/TLB-shadow theory was a flag-decode error, the merge agent is ABSENT IN EVERY LANE (walk-time A/D deltas are the only post-carve changes, host-side softmmu, invisible to instruction watchpoints), the storm is pure stack-boundary geometry over a static SEC-carved moat; instrument walls named: gdbstub watchpoint desync, conditional-breakpoint desync (3/4 vs 3/3 unconditional), the observer effect (per-delivery freezing defuses the storm), walk-time A/D blind spot |
| `findings-thirty-second-ring.md` | l'instant en vol — the storm caught IN FLIGHT and the species genome: front 32a closes ring 31's frozen-instant residual with a passive instrument (QEMU `-d int` logging — nothing freezes, the storm fires): the full fatal ladder captured live (543 consecutive re-deliveries at CpuDxe+0x1021 descending exactly 0x660 per step, zero below the moat, the 544th stub push 0x3A8 crossing at fxsave CR2 = moat−0xA0), the mechanism unified end-to-end (chained re-deliveries + partial unwinds = an oscillating walk at the boundary, fate decided per delivery by TCG scheduling), and THREE measured corrections (ring 31b's "deterministic" was BUILD-scoped — the ring-32 rebuild storms 3/8; the "byte-identical CR2" is QUANTIZED-NOT-UNIQUE — {moat−0xA0, −0xD0, −0x180}; the SMMSTORE lane NEVER boots green — its only end states are fatal crossing or indefinite cascade oscillation, longest observed run 513 deep and RECOVERED); front 32b fuses nine rings of registers into the study's first species genome (`vendor-genome.json`: 330 species ever touched by a frontier event, births EXACTLY the two security waves, deaths EXACTLY the two legacy retirements, SbRomArmorSmm moves=0 at genome granularity, 26 chronic movers at every frontier, day-0 GUID placement table) |
| `findings-thirty-third-ring.md` | la loi de l'atterrissage + l'arme du jour 0 — front 33a the day-0 TRIAGE WEAPON (`fw33-triage-engine.py`, the study's first deliberately TRACKED instrument, survives the sandbox): seven registers fused into one queryable engine (guid/body/rung/plan/wave/agesa/selftest/manifest), 29 genome-exact self-test gates with loud refusal on register drift, three register-drift lessons caught by the gates before they could become silent errors, genome-exact demos (SbRomArmorSmm placement, the 4655 bench-day card); front 33b the LAW OF THE LANDING: five storms over two builds (ring-33 build 2/24 + the surviving ring-32 logs 3/8, 27 oscillation hangs, passive `-d int`) yield three closed-form formulas verified 5/5 — the fatal window has width EXACTLY one chain step (0x3A8 push + 0x2B8 write offset = 0x660), so exactly ONE delivery per chain is fatal, forced by the onset SP alone (k* = ⌊(onset−moat+0x2B8)/0x660⌋+1 = 543 invariant on the observed onset band), landing = onset − 542×0x660, residue = 0x3A8 − ((onset−moat) mod 0x660) — the residues {0xA0, 0xD0, 0x180} ARE the recovery-alignment classes {0x308, 0x2D8, 0x228}; hangs recover from runs up to 542 deep (one step short), both terminals are kills (fatal-crossing-then-SILENCE measured), and the second build's rate (2/24) confirms build-scoping with a second data point |
| `findings-thirtieth-ring.md` | the storm and the static moat: the three ring-29 micro-threads closed by measurement — front 30a the storm window as a RATE (2/40 fresh RELEASE+EMU boots, pooled 3/46 ≈ 6.5%, NO coreboot precursor, pure TCG-scheduling race inside DXE dispatch); front 30b the ghost watchpoints on PDE[38]@0x4803130 / PDE[39]@0x4803138 catching FOUR writes — the 2-MiB fill, the 4-K PT fill, and THE RO-clear pass — all three fingerprint-matched into the payload's SECURITY_CORE (SEC, 32-bit, pre-paging at 0x800000), then ZERO writes to S5: the merge agent does not exist in the EMU lane, the moat is static, the storm is stack-boundary geometry (sixth 0x660 nesting crosses into the 2-MiB RO direct map) — ring 29's TLB-shadow/merge-back theory FALSIFIED for that lane (SMMSTORE-lane residual registered); front 30c the AuthcAMDenti owner named at three levels: byte (cmpl-literal rollup → DxeCore/CpuDxe/HpetTimerDxe), source (BaseCpuLib `StandardSignatureIsAuthenticAMD`), runtime (CpuDxe `SetMemoryAttributes`→MtrrLib interrupt-restore boundary, MtrrLib.c:354) |
| `findings-thirty-fourth-ring.md` | the differ, re-derived — the stale-clone reconciliation (evidence commit preserved as tag `thirty-fourth-ring-local`; the redundant volume-5 prep dropped, rings 24-26 already rehearsed past it) and four blind re-derivations that agree with the ledger to the digit: ring 4's rebuild wall (126 common, 6 identical, +18/-9), ring 5's enrollment-is-data at whole-image granularity (sha256 identity classes: secboot == snakeoil == ms), ring 3's three NX bytes (BdsDxe 01→00 + the IScsiDxe NOP-shift fingerprint), ring 2's VARS census (21 live / 18 not-live) decomposed one byte deeper (17×0x3C + 1×0x3D — the replaced BootOrder with no live successor, registered) |

Every findings file carries the same honesty ledger: what is proven,
what is flagged, what stays an open question — and ends with its
consequences for day-0 (16/09). The ring-5 opcode lock was resolved by
ring 6 (spec-fresh grammar); ring 6's list-header lock was resolved by
ring 7 (a category error — the list never ships); ring 7's last
registration (pkg3) was dissolved by ring 8 (a decoy anchor nested
inside the real package — the flat count double-counted 6 patterns);
each ledger self-corrects where the answer lands.

### The artifacts (JSON, the evidence)

| File | Produced by | What it is |
|---|---|---|
| `ovmf-census.json` | first ring | the module census of the real OVMF builds (PenetrateLZMA → FFS walk) |
| `ovmf-keyring.json` | second ring | the NVRAM trust store decoded: PK / KEK / db / dbx, 39/39 records, X.509 subjects |
| `ovmf-kill-list.json` | third ring | the GUIDed amputation lists: network 21 modules, storage 18, USB 6, display 6 |
| `ovmf-build-delta.json` | fourth ring | the FFS hash matrix across builds — the only census that sees policy |
| `ovmf-pe-provenance.json` | fourth ring | PE header provenance: zero timestamps, no PDB paths, the NX badge |
| `ovmf-ms-delta.json` | fifth ring | secboot vs ms vs snakeoil: 144/144 modules byte-identical — trust is configuration |
| `ovmf-ifr-grammar.json` | fifth ring | the HII package-list hunt: what validated, what stays locked (opcode layer) |
| `ovmf-acpi-footprint.json` | fifth ring | checksum-validated ACPI/SMBIOS scan of 5 builds: templates in builders, zero finished tables |
| `ovmf-ifr-census.json` | sixth ring | the per-module IFR census: questions / options / pages per setup module, validated by exact consumption |
| `ovmf-ifr-facade.json` | seventh ring | the facade rendered: formsets / forms / questions with resolved text (en-US + fr-FR), options with values, varstores with names and sizes, defaults, per-question conditions |
| `ovmf-depex-dag.json` | eighth ring | the dispatch graph: postfix-evaluated DEPEX classes, hub fan-in (PcdProtocol 62, DevicePathUtilities 54…), the apriori declared order per FV, the cross-build gate delta (changed-while-present = {}) |
| `ovmf-scsu-strings.json` | eighth ring | the SIBT block census (SCSU = 0 real blocks; SKIP2 only in fr-FR; header-faithful closes 110/110), the UTS#6 decoder + 5/5 self-tests, the observed string-header bytes, the id-resolution closure (id 0 = null marker, cross-module matches = collisions) |
| `ovmf-pkg3-head.json` | eighth ring | the pkg3 autopsy: anchor and shift grids, hexdumps, the nested-span proof, the covering STRING record, the real formset guid byte-equal to the tianocore source, the 208 = 202 + 6 accounting |
| `ovmf-guid-names.json` | ninth ring | the nameplate: every GUID in the 13 artifacts resolved (618/618, 209 distinct) against 11 .dec inventories + census + kill-list + the edk2 master tree; the closes that retire ring-8 "?"s (PcdPeim apriori entry, File Explorer formset, EnrollDefaultKeys-as-owner) |
| `ovmf-siglist.json` | ninth ring | the trust store unpacked: PK/KEK/db/dbx record-by-record with named owners and X.509 fingerprints, the sha256("") dbx placeholder verified by value in both enrolled stores, the synthetic 1000-entry scale rehearsal (2.3 ms, exact) |
| `ovmf-weight-map.json` | ninth ring | the frugality ledger in bytes: per-FV accounting (header/data/padding/tail) on 3 builds, named top consumers (TlsDxe ≈ 1 MB, Shell 894 KB), the by-name swap delta (+18 SMM modules − 9 incl. the Shell, net +810 KB), the FvbServicesSmm false-FV dismissal |
| `ovmf-rehearsal.json` | tenth ring | the general rehearsal record: the 14-stage chain (census → keyring → build-delta → pe-provenance → ring5 → ifr-census → facade → final → depex → scsu → pkg3 → guid-names → siglist → weight-map) timed on 4 specimens with pre/post canonical-content comparison, per-stage churn data, verdict REPRODUCIBLE — the ring-9 claim "only new inventories on the vendor image" proven, not asserted |
| `vendor-specimens.json` | eleventh ring | the first vendor-silicon register: two AMI-Aptio AM4 flash maps (MSI 32 MB dual-structure, Gigabyte 16 MB) with nesting-rule-validated FVs, the pierced census (453/498 modules by phase, UI-named shares, AGESA byte-verified where present), the cross-vendor GUID intersection (275 shared, jaccard 0.549 — the "same bones" claim measured), the 100 % DEPEX parse with PcdProtocol hub dominance (202/270 fan-in), and the acquisition honesty (ASRock 403-blocked, ASUS deferred to the day-0 dump; both entries carry visible ring-12 corrections) |
| `vendor-acquisition.json` | twelfth ring | the sourcing ledger: per-vendor latest-official verification (ASUS GetPDBIOS API 39-entry ledger, MSI/GB/ASRock official listings), verbatim URLs and full zip sha256 for all five acquisitions, the blockage stories with their ways found (WAF header-set completeness, canonical API naming), the 2,048-byte CAP wrapper proof, and the 3644 question with its decision path |
| `vendor-quorum.json` | twelfth ring | the quorum evidence: ASRock and ASUS census summaries (top FVs, phases, named shares, DEPEX 100 % with hub tables, PSP/BSP hits, AGESA verification status), the packaging lens (ASUS 384 VERSION / 1 UI section — the nameplate blindness measured), and the pairwise GUID-identity matrix across the four vendors plus the OVMF reference rows (41–43 shared each) |
| `vendor-versions.json` | thirteenth ring | the VERSION-strings lens, measured: the decode convention decided by bytes (1,992 main-region sections, 100 % PI-spec str@2), the vocabulary that refutes the ring-12 candidate (8 distinct strings, seven placeholders — '1.0' ×1,860), the GUID join that recovers the names anyway (665-GUID map, 2 pseudo-conflicts, ASUS 4655 0.002 → 0.593 named — DXE 0.83, SMM 0.86), the UefiRaid case (C74F06D2: '1.0' → '9.3.0.00308', the one version change in four years, caught up with ASRock/GB/MSI), the 600-row ASUS named-module register for day-0, the MSI mirror asymmetry (+16 files, 0x97000 bytes longer), and the cross-release packaging stability (414/415 strings identical 2022→2026) |
| `vendor-psp.json` | thirteenth ring | the PSP lens: magic scan with $BSP/$BIO re-scanned to zero (the verified magics are $PSP/$PL2 and $BHD/$BL2 — the ring-11 zero closed by source), 74/74 directories fletcher32-validated and cross-checked against psptool 3.6 (type tables imported at runtime, never transcribed from memory), the full ASUS PSP/BIOS directory trees per release, the cross-release churn (37 blobs identical, 38 changed — bootloader, TOS, SMU, ABL0-7 move while some instances survive byte-identical), the constants (PspFtpmHandler everywhere, DRAM/HMAC/RPMC key machinery in the bootloader strings), and the psptool 32-MiB address-label join lesson |
| `vendor-nvram.json` | fourteenth ring | the NVRAM lens: the AMI NVAR store, first parse across six specimens — FFS-anchored store → one outer StdDefaults variable → nested defaults store (11–18 variables per board), the backwards GUID area self-tested by the UEFI spec's own variables, the factory canon (9 names shared by all six) with per-vendor tails, the MSI mirror delta (Setup 1,428 vs 1,972 B), zero walk errors / zero broken links / clean terminators on 14 walks, and the day-0 delta protocol (live minus factory) |
| `vendor-ledger-tuf.json` | fourteenth ring | the 3644 evidence file: the ring-11 "3644 zip" proven a 240-byte HTML error page, three official ASUS ledgers checked (PRIME B450-PLUS 39 releases, TUF B450-PLUS GAMING 33, TUF B450M-PLUS GAMING 33 — 3644 in none), and the TUF 4645 acquisition with the project's first vendor-certified sha256 (the API's own per-release hash, matched byte-exactly; CAP wrapper proof reproduces on a third release) |
| `vendor-release-delta.json` | fifteenth ring | the release-delta lens: PRIME B450-PLUS 3604 vs 4655 diffed at FFS-body sha256 granularity (outer AND pierced-inner volumes), the 616-GUID delta (285 unchanged / 322 changed / 7 added / 2 removed, 46.3 % untouched), the changed-by-phase histogram (DXE 201, SMM 65, freeform 42), 269 changed modules named by the 532-GUID quorum join (53 recorded as GUIDs, never invented), the seven added flash-armor modules, the one-byte 80,232 → 80,231 raw change, the NVRAM anchor cross-check (same size, different content), and the honesty entries (body-hash vs header bits; two releases measured of 39; AGESA level strings not recovered) |
| `vendor-day0-chain.json` | fifteenth ring | the second generale: the six-stage vendor chain (unwrap → census → versions → PSP → NVAR → depex) in protocol order, two full runs, per-stage exit+existence verdicts and chain identity judged at the end — run 1 COMPLETE-NOT-IDENTICAL (the ritual catches set-iteration key order in versions.phases_delta, the ring-10 species returning on schedule), fix at source, run 2 REPRODUCIBLE (all six registers byte-identical, ~8 s per chain), and the reading: the 16/09 dump joins this chain as a seventh specimen in the first hour |
| `vendor-agesa.json` | sixteenth + eighteenth rings | the pierced AGESA lens: every specimen's AGESA level byte-verified from raw AND LZMA-decompressed content (ASUS 4655 = 1.2.0.12, 3604 = 1.2.0.6b, TUF 4645 = 1.2.0.F — the four ring-11/12 'unverified' blanks closed; MSI 1.2.0.8, Gigabyte 1.2.0.12, ASRock 1.2.0.E all MATCH their vendor claims — zero vendor lies detected), the strings located 2.7-2.9 MiB into the decompressed 0x9c0000 payload (the raw scan could never see them), Gigabyte's SMU side-harvest (3.4.1.1-3.4.2.4, fourteen distinct), and — extended by ring 18 — the nine-point dated PRIME chronology (3604=1.2.0.6b, 3802=1.2.0.7, 3810=1.2.0.7, 4003=1.2.0.8, 4202=1.2.0.A, 4402=1.2.0.B, 4604=1.2.0.Ca, 4631=1.2.0.E, 4655=1.2.0.12 — a dump trapped between two rungs is dated between their releases; AGESA alone does not separate 3802 from 3810) |
| `vendor-flash-armor.json` | sixteenth + eighteenth rings | the flash-armor bisect: the official API ledger re-fetched (40 releases, 0318→4655), interrogated with 7 downloads + 2 free sentinels, per-probe presence sets written after every probe — ring 18 spent the three withheld downloads and the answer became exact: core SMM armor 5/7 from 3802 (2022-05-12, the release immediately after 3604; the ring-16 'brackets the LogoFail year' reading corrected — the armor predates the disclosure by ~14 months), complete 7/7 at 4604 (2024-04-08), the legacy SMM retirement staged across the disclosure year (2/2 at 4003 → 1/2 at 4202 → 0/2 by 4402), the sampling-bias method lesson registered, and the day-0 checklist: five GUIDs expected from 3802, seven from 4604, legacy pair 2/2 if ≤ 4003 |
| `vendor-byte-forensics.json` | sixteenth ring | the WHAT behind ring-15's WHO: same-size changed modules diffed byte-by-byte (GUIDs read from the ring-15 register, never from memory) — AcpiTableDxe's 9 bytes in 8 surgical spans, ACPI's 58.5 % rewrite at constant size, the NVRAM anchor's 778 B landing exactly in the HWM/QFan default region, and 1DF36FF9 exposed as a wholesale replacement (0 % overlap after a 16-B prefix) that the 'one-byte size delta' story completely misdescribed; span geometry structural, never a disassembly |
| `vendor-msi-mirror.json` | seventeenth ring | the MSI double structure, root-caused: two 16-MiB SPI windows measured (the half-delta voted by the paired small FVs), the main-FV compare (380 shared: 261 identical / 119 changed; 69 + 85 unique, named — the family split), the three fletcher-valid combo tables (PSP2 x2, BHD2 x1, per-chip IDs and resolved `$PSP`/`$BHD` targets, 20 rejected magic coincidences recorded), the 18-directory re-assembly, the 84-pair span forensics (42,441 B in 3,081 spans), the Setup-defaults diff (1,428 vs 1,972 B: 121-byte prefix drift in 28 spans + a dense 544-B tail), the sources (doc #55758 via coreboot, fetched to disk), and the honesty entries (PSP-ID semantics opaque, boot-window selection not file-measurable) |
| `vendor-pe-trust.json` | nineteenth ring | the PE-provenance + trust register: per-specimen PE module stats (machines, subsystems, zero stamps, the MSI 2012/2014 fossils, the NX inversion 0/5707), and the factory trust material — zero signature lists anywhere, the raw Authenticode DER chains with the ASUS signer (TW 23638777) and its timestamp chronology 2021→2025 |
| `vendor-geometry.json` | nineteenth ring | the entropy atlas of the raw images: 64 KiB-window classes per specimen, the ASUS skeleton priced for day-0 (FV share, packed share, 0xFF slack), the MSI 32-MiB half-emptiness, the packed-share creep 55.9→60.3 % as the aging signal |
| `vendor-lifecycles.json` | nineteenth ring | three lifecycles on one board family: the PSP blob churn table across the nine rungs (42/50 at 3802, 7/50 at 3810), the armor anatomy (SbRomArmorSmm one body, PrepareWhiteListSmm's chip DB, the SMM census 106→109→107), and the CVE patch lens (F65→F67c: 18.1 % untouched, the fix non-isolable inside a platform rebuild) |
| `vendor-ifr-census.json` | nineteenth ring | the vendor facade in questions: per-module forms/packages/pages/questions/options for six vendor-representative specimens (6,360-8,646 questions, MSI the largest, CBS dominant), 100 % exact-consumption validity, and the near-zero condition count that relocates the hidden surface to variable level |
| `vendor-clocks.json` | twentieth ring | the five dating clocks fused: the nine-rung table with AGESA / DER cert set / SMM armor / PSP state / geometry side by side, the pair-discrimination matrix (3802|3810 separated by NOTHING but the cert set), the minimal resolving clock pairs computed — (cert_count, AGESA) pins all nine rungs — and the cost-ordered day-0 decision tree (geometry → certs → AGESA → armor → PSP) |
| `vendor-smm-quorum.json` | twentieth ring | ring -2 across four vendors: SMM censuses by PI file type gated on the ring-19 register (both ASUS gates exact), pairwise SMM jaccard (intra-vendor 0.954, cross-vendor lower), the 83-GUID all-5 spine, the armor GUID presence matrix (SbRomArmorSmm 51080191 / PrepareWhiteListSmm 391626DB / FlashSmiSmm 6C289241 / FlashSmiDxe 755877A6 present on all four vendors, absent from ASUS 3604), the armor body identity (3 SbRomArmorSmm bodies; MSI=ASRock byte-identical), and the OVMF overlap (4 GUIDs per vendor) |
| `vendor-unasked.json` | twentieth ring | the hidden surface priced in bytes: per-vendor varstore coverage by question (offset,width) unions — 84-90 % of varstore bytes never referenced by any question, the Setup blob's unasked 142-567 B per board (the setup_var target priced), the zero-question varstores, the IFR-side Setup sizes matching ring 14's NVRAM side; gates reproduce ring 19's totals exactly before any coverage number was read |
| `vendor-psp-mirror.json` | twentieth ring | the MSI mirror at the PSP layer: the upper 16-MiB window parsed standalone for the first time (10 dirs vs the lower's 8; psptool's FET walk misses it), the table-structure mirror (same magics/counts/sizes at +0x1000000, 0/10 byte-identical tables — addresses relocated), and the blob verdict: 72/76 paired (magic,type,subprogram) keys CHANGED at identical sizes, 4 identical (incl. the 4,096-B 0x22 policy table), 33 family-A types lower-only — 3.7 % byte-stability: a parallel build, not a copy |
| `vendor-microcode.json` | twenty-first ring | the sixth clock: psptool BIOS-directory 0x66 microcode census on twelve specimens, gated on the ring-16/18 armor register (9/9 PASS) — exactly 19 patches per specimen (the carrier constant), monotone newest-patch dates 2021-07→2024-08 across the ASUS ladder, the ladder diffs (the 2024 refresh replaces the 2019 generation at 4631), the quiet 3802|3810 pair NOT split (identical sets), the resolving-pair recomputation keeping (cert_count, AGESA) alone on top, and the cross-vendor microcode eras (MSI=2022-03, Gigabyte/ASRock=2024-08-22 — the AM4 set frozen since 4631) |
| `vendor-hidden-map.json` | twenty-first ring | the hidden map: ring-20's unasked-bytes machinery inverted into merged [offset,length) ranges per Setup carrier, gates reproducing ring 19's totals 5/5 — ASUS 456 B with 142 B never-asked in 27 ranges IDENTICAL at 3604 and 4655 (the Setup layout frozen across 4.5 years), MSI 1,428 B/567 B/166 ranges (the most hidden and the most fragmented), Gigabyte 628/247/110, ASRock 677/260/71; knowledge-only — the zero-write doctrine untouched |
| `vendor-acpi.json` | twenty-first ring | the ACPI lens: checksum-admitted table inventory per specimen (position classifies, the checksum admits) — the vendor ships the AML only (4 DSDT variants + 18-23 SSDTs per board, checksum-valid raw sections in the compressed FV) and builds every static table (zero FACP/APIC/MCFG/HPET/IVRS bytes anywhere; the OVMF-imported gate refuted by the debug probe and rewritten), the DSDT clock (three body generations 27d5e826→f23c571b→0a4a6f16 moving at 4202→4402 where the DSDT shrinks 2,141 B — the legacy-SMM retirement release — and 4604→4631; compiler INTL 2014-09-25 static), and zero table bodies byte-identical across all twelve boards |
| `vendor-ssdt-clock.json` | twenty-second ring | the eighth clock: every checksum-admitted DSDT/SSDT body hashed across 13 specimens with container classification — the SSDT SET moves at 3604→3802 (one AOD table, rev 15→153, the armor release) and 3810→4003 (the wholesale 15-body CPM-family rewrite, AGESA 1.2.0.8), frontiers DISJOINT from the DSDT clock's; the ring-21 RAW-SEC census reconciled exactly (4+23) plus the 8 EMBEDDED ALIB bodies ring 21's model could not see — 74 ALIB bodies corpus-wide, 6 byte-identical across all 13 boards (AMD's common layer, refining the no-shared-body claim); cross-vendor AML sharing deduped (TUF 31/35, MSI 28/35, Gigabyte 6/35 — the most divergent) |
| `vendor-der-inventory.json` | twenty-second ring | the collapsed census: the raw-plane 12/18 "certs" are SIGHTINGS — each trust certificate ships 3× (PK/KEK/db stores); the factory material is 4 distinct certs at 3604-3802 → 6 at 3810+ (gated 9/9, cross-vendor zero 3/3), and the count never changes again while the BODIES rotate — the DigiCert timestamp chain re-issues at five frontiers (2021→2025) and the ASUS leaf renews at 4631 (2024-05-27); PRIME 4655 = TUF 4645 identically; the WIN_CERTIFICATE/PKCS#7 AUTHENTICODE class 1-3 per board; negative serial numbers on vendor-generated certs (ASUS SW Keys, MSI SHIP PK, GIGABYTE) — an RFC 5280 violation shipped by three vendors |
| `vendor-armor-chipdb.json` | twenty-second ring | the frozen whitelist: the 'AMD rom armor' banner anchors a 46-family SPI chip-name table — byte-identical across all five vendors, zero set changes from 3802 through 4655 (4.5 years); 3604 carries the 41-family predecessor (the whitelist belongs to the AMI flash driver and predates the armor), expanded +5 at the armor release (Cypress 25FS-S, Fudan FM25W, MXIC 77L, XMC 25RH, XMC 25RU); name-granularity census, per-chip binary command tables unparsed — knowledge-only |
| `vendor-churn-atlas.json` | twenty-third ring | the module-body time axis: ~613 FFS modules per specimen hashed across the nine ASUS rungs + TUF — the frontier table (born/dead/moved per transition), the armor birth certificate module-exact (quintet at 3802, +2 at 4604, joined by GUID to the ring-20 quorum matrix), the legacy SMM deaths dated (827E45A4 @4202, 21782819 @4402), the quiet pair quietest (45 moved), TUF vs PRIME (473/613 identical, 0 uniques); gates: armor quartet 4/4 PASS, wave-2 count 2/2 PASS, legacy deaths 2/2 at the registered rungs |
| `vendor-psp-authorship.json` | twenty-third ring | the PSP blob authorship corpus-wide: 13 specimens, independent fletcher32 admission (reproduces ring 15's MSI 18/18 and ring 20's 10-table upper mirror), 2,900+ entries classified — 3 universal bodies only, 105 keys shared by 12/13 (all but MSI), 26 keys by asus-4655+gigabyte, 355 singletons, 335 vendor-private, 103 ASUS-AGESA-keyed; the registered "211 shared PSP blobs" adjudicated DEAD METADATA under five explicit scopes (133/112/145/201/186) with the qualitative register intact (223 entries, 201 identical with multiplicity) |
| `vendor-genome.json` | thirty-second ring | the species genome of the ladder: nine rings of registers fused into ONE queryable catalog (zero downloads) — 330 species ever touched by a frontier event, each with birth rung, death rung, move count, body-hash chain and armor flags; births EXACTLY the two security waves (5 @3802, 2 @4604), deaths EXACTLY the two legacy retirements (4202, 4402); the move distribution (98 species at 5 moves, 26 at every frontier = the AGESA engine room), SbRomArmorSmm moves=0 at genome granularity, the quiet pair confirmed (0/0/45), and the day-0 GUID placement table with its honesty block (touched-species scope, names join only at the armor quartet) |
| `vendor-vocabulary-clock.json` | twenty-third ring | the setup ladder in words: per-module string sets (ring 7 discovery + ring 8 UCS2/SCSU decode, closure-gated) diffed along the nine rungs — 27 modules / 29 packages / ~5K distinct strings per rung, the frontier word table with examples (`PSP RPMC Switch` born 3802 with its NOT-FOR-PRODUCTION stamp; +32 disk-slot templates at 3810; Stability Boost at 4631), the churn cross-read (body-moved vs words-changed per transition — 224 movers, zero words at 3810→4003), and the quiet pair's symmetric diff |
| `vol5-qemu-photograph.json` | twenty-fourth ring | the first before/after photograph: OVMF plain/secboot vs coreboot.rom (same machine, QEMU q35) — spi-map JSON untouched, cbfstool CBFS layer, census cross-citation by sha256, the container-format delta |
| `vol5-cycle-rehearsal.json` | twenty-fourth ring | the doctrine's flash cycle rehearsed file-level: dump-twice/two-media, identify, write+verify, the flipped-byte failed-verify branch caught, rollback from the second medium — 5/5 pass |
| `vol5-board-matrix.json` | twenty-fourth ring | the sacrificial-board matrix verified against the coreboot 25.12 tree: azalea = Framework 13 AMD 7040 (primary for the coreboot lane), System76 13 models, x230 the school, T440p and X470D4U absent from 25.12, B450-PLUS control-only, h610i-plus-d4 the consumer-ASUS lane |
| `vol5-cbfs-census.json` | twenty-fifth ring | the CBFS lens as code: FMAP + CBFS parsed spec-fresh from the 25.12 tree headers imported at runtime (master header big-endian, found via the last-4-bytes little-endian pointer, wrapped in a cbfs_master_header entry), 13 entries with attribute chains, per-entry sha256, LZMA decompression in-probe, the payload segment table (CODE LZMA @0xDE060 + ENTRY @0xFD25A), alignment gaps all below 64 B and the free space filling the region tail exactly — 13/13 identical to cbfstool AND to the ring-24 photograph, plus the provenance verdict (ROM config = defconfig + `cc0358747d2a-dirty` stamp, not the olddefconfig expansion) |
| `vol5-qemu-boot.json` | twenty-fifth ring | the first dynamic photograph: the study-built coreboot.rom booted under QEMU q35 (tree's own documented command; TCG, no root, exit 0 in 73 s) — the four-stage chain with the BS: millisecond timeline, SeaBIOS rel-1.17.0 payload, the expected no-bootable-device branch with zero disks — and the runtime's independent confirmation of the static census: 4/4 CBFS fetches at identical offsets and sizes, mcache 13 files = lens 13 entries |
| `vol5-os-boot.json` | twenty-sixth ring | the chain evidence: both firmware worlds booted to a real userspace (markers `FW26 firmware=bios|uefi`, `FW26 CHAIN OK`, clean S5 power-down), specimen sha256s (rom, disk, kernel), the EFI-stub initrd line as the OVMF-side proof, and the debug history (syslinux/GRUB/superfloppy) kept in the factory scripts |
| `vol5-ab-timing.json` | twenty-sixth ring | the first A/B of the study: 3 runs per side, medians, per-line timestamps, segments (first byte / kernel start / userspace), the registered asymmetry (native loaders by construction) and the TCG/OVMF-proxy honesty ledger |
| `vol5-fw27.json` | twenty-seventh ring | the silence decomposed and the study's own stub: 5 registered root causes with the v6.12 source evidence (the inert UKI sections, the `efi_random_alloc` knife-edge behind the misleading "Failed to decompress kernel", the empty-LoadOptions mute boot, the CHAR16-only LoadOptions conversion, the LoadFile2 `EFI_BUFFER_TOO_SMALL` probe contract), the fw_cfg RAM floor ladder (128✗/192✓), the full disk-probe matrix, and the FW27 stub provenance (sha256, gnu-efi/gcc/objcopy recipe, 1.1 MiB vs the 13.2 MiB UKI) with the 3/3 green chain trail witnessed line by line |
| `vol5-fw28.json` | twenty-eighth ring | the world-naming stub and the dual-world recipe: FW28 design (identity anchors, why SMBIOS type 0 is the discriminator — the payload says EDK II in BOTH worlds), the 3+3 green matrix with the on-the-wire verdicts and kernel-side confirmation lines, the full scratch resurrection recipe (iasl/lib32gcc/wrapper-under-two-names/xcompile), the six walls (28-w1..w6) with fixes and lessons, the edk2 pin decision, and the honesty register (FW26-vintage markers, inherited OVMF logs, the AcpiPlatform abort, the stack-RO sub-question open, EMU variables labeled) |
| `vol5-fw29.json` | twenty-ninth ring | the RELEASE lane closed and measured: the 29b chain of evidence (qemu `-d int` event series with the 0x660-step stack descent, the RELEASE dump's CR2=RSP and CR0.WP=1, the fxsave/EnableInterrupts disassembly, the gdb post-mortem PDE walk proving the stale-translation shadow, the preserved frames with the `AuthcAMDenti` CPUID-vendor buffer and the `cmpl "Genu"` caller), the HPET vector identity (`PcdHpetLocalApicVector`=0x40, LocalApicTimerDxe absent from the FV — the 32-vector decoy named), the 29a RELEASE+EMU matrix (5/6 green, 1/6 storm, stub RELEASE-grade, the w2 gate re-armed), the 29c source verdict (AcpiPlatformDxe FV-storage vestige, line-191 EFI_ABORTED, benign), and the honesty ledger (storm sample size, the run-2 missing -d int capture, the module-class-not-file naming) |
| `vol5-fw31.json` | thirty-first ring | the storm caught live: the 22-boot watched sample (hunt + parallel catchers on three ports), the deterministic SMMSTORE lane rebuild recipe and byte-identical reproduction, the three frozen sessions (phase-1 watchpoint stops A-D, the fxsave-breakpoint HIT1 with legal RSP, the 64-delivery oscillation with the 0x660 step visible), the four-part verdict (ring-29 decode error corrected — RW=0 everywhere, agent absent in every lane, ring-30 verdict extended, storm = geometry), the four instrument walls, the lane-restore recipe with the green sanity boot, and the honesty ledger (frozen pre-fatal instant = registered residual, D-on-fault QEMU subtlety, drift #16 write-off) |
| `vol5-fw32.json` | thirty-second ring | the storm caught IN FLIGHT: the passive `-d int` instrument (no freezing — the observer effect applies to stopping the CPU, not to logging), the 8-boot sample (3 storms / 5 cascade hangs, rate 3/8), the fatal ladder measured live (543 × −0x660, zero below the moat, the 544th stub push 0x3A8 → fxsave at moat−0xA0), the hang anatomy (at-RET runs up to 513 deep and RECOVERED, SP bands matching ring 31b's oscillation), the three measured corrections (build-scoped determinism, quantized-not-unique CR2, the lane never completes DXE), the ring-32 lane recipes, and the honesty ledger (drift #17 write-off, logging-conditioned rates, one-build scope) |
| `vol5-fw33.json` | thirty-third ring | the law of the landing + the day-0 weapon: front 33a the tracked triage engine (29 gates, modes, drift lessons, demos, survival rationale), front 33b the 32-boot census (24 fresh + 8 surviving ring-32 logs; 5 storms / 27 hangs) with the three closed-form formulas (depth forced by onset via the exactly-one-chain-step fatal window 0x3A8+0x2B8=0x660; landing = onset − 542×0x660; residue = 0x3A8 − alignment), the per-storm geometry table, the 542-deep recovery census, the terminal-state refinement (fatal-crossing-then-silence), the second-build rate (2/24 vs 3/8 — build-scoping confirmed), walls, recipes and the honesty ledger (drift #18 write-off, small-sample honesty, lane-scoped constants) |
| `fw33-triage-engine.py` | thirty-third ring | the day-0 triage weapon itself, TRACKED in lab/ (first ring instrument to survive the sandbox): stdlib-only register fusion over the seven lab registers, eight query modes (guid / body / rung / plan / wave / agesa / selftest / manifest), 29 genome-exact gates with loud refusal on any drift (AGESA pin at load, gates at self-test), chain-integrity check across all 330 species, the three register-drift lessons encoded as gates |
| `fw35-oracle.py` | thirty-fifth ring | the oracle scorer, TRACKED in lab/ (the fw33 precedent): stdlib-only, five modes (list / show / template / score / manifest) behind a 36-gate selftest that re-derives every basis number from eleven persisted registers and refuses loudly on any drift; four-verdict scorer (hit/partial/miss/na) with registered checkers for the relational predicates (identity H3, PSP overlap direction, AGESA monotonicity, no-third-wave, no-births-no-deaths, conditional DSDT); exit 1 on any miss, exit 2 on register drift |
| `fw36-atlas.py` | thirty-sixth ring | the multi-scale atlas + delta cartography, TRACKED (the fw33/fw35 precedent): stdlib-only, modes atlas/delta/vars/manifest behind a TWO-TIER selftest (tier R re-derives anchors from the ring-34 registers always; tier I re-derives them LIVE on the surviving corpus and skips loudly when absent — 14/14 PASS with tier I live); raw _FVH scan with checksum validation, gap-clustered byte runs, structural classes from window-entropy pairs (erased->code materialization, code->code in-place, code->erased retraction) |
| `vendor-triage-engine.json` | thirty-third ring | the engine's manifest: input register anchors (sha256_16 each), the 29-gate self-test transcript (all PASS), coverage stats (330 species, 1536 body-hash anchors indexed, 7 named joins), and the demo transcripts (SbRomArmorSmm placement, wave2 freeform placement, the 4655 bench-day plan card) |
| `vol5-fw30.json` | thirtieth ring | the static-moat closure: the 40-run Monte-Carlo protocol and classification, the four watchpoint stops (A/B fill, C PT fill, D the RO writer at SEC+0x42bc) plus the measured absence, the byte-fingerprint resolution into UEFIPAYLOAD.fd SECURITY_CORE, the revised mechanism (static moat, deterministic #PF(0003) geometry, no split/merge), the 30c three-level naming with the source anchor, the lane-scoped residuals (SMMSTORE-lane table state, storming-boot watchpoint follow-up), and the honesty ledger (binomial width, inferred crash-time PDE, bit-arithmetic reinterpretation, sandbox instruments) |
| `dhm-closure.json` | seventeenth ring | the ring-8 curiosity closed: the two DriverHealthManagerDxe formset GUIDs censused across every module of three OVMF builds — the carrier set is DriverHealthManagerDxe itself, BdsDxe (x1) and UiApp (x1, 462CAA21 resolved from UiApp.inf on disk); context forensics show code-adjacent rodata after x86 NOP padding (a compiled constant, not an HII string collision) and UiApp keeps it next to its own formset GUID block; the verdict is intentional runtime formset routing, with the honest limit registered (the on-disk edk2 tree is not the Debian build tree — the byte verdict does not depend on it) |
| `ovmf-constellation.json` | thirty-fourth ring | the standing differ, layer 1+2: whole-image sha256 identity classes (3 classes from 5 builds) + GUID-aligned module ledgers per pair (only-in lists, changed counts, byte deltas) with the ring-34 renumbering and per-ring cross-citations in _meta |
| `ovmf-nx-pin.json` | thirty-fourth ring | the strictnx delta pinned by an independent walker: 2 modules, 3 bytes, run-clustered with before/after hex — ring 3's front T2 re-derived blind |
| `ovmf-vars-differ.json` | thirty-fourth ring | the VARS walk re-derived with the 60-byte auth header (no PublishSequence, derived on live bytes): per-record states (0x3F/0x3C/0x3D), latest/live views, the PK/KEK/db/dbx table, the dbx cross-image invariance, the BootOrder 0x3D nuance |
| `vendor-oracle.json` | thirty-fifth ring | l'oracle du jour 0 — the FROZEN prediction register: 26 falsifiable predictions across two targets (day0-3644 dump: 19, release-41: 7), each with kind, expect/widened bands anchored on the board's OWN measured values (not corpus ranges — the 84-90% transplant error caught pre-freeze on P-16), confidence, per-register basis citations and an explicit falsifier; scoring semantics hit/partial/miss/na, protocol = fw35-oracle.py score only; question_3644's H3 is the registered identity outcome (P-18) |
| `findings-thirty-fifth-ring.md` | thirty-fifth ring | the oracle ring report: the twenty-six predictions and their derivation laws, the board-anchored band lesson, the four-verdict scoring semantics, the synthetic demo (19 hit / 7 na, session-side, labeled DEMO), and what day-0 becomes — a measurement against registered expectations where every miss localizes a law |
| `ovmf-geometry.json` | thirty-sixth ring | the first OVMF-side geometry: FVMAIN_COMPACT + SECFV checksum-valid on every CODE build, plain 52.02/43.05 erased/packed vs secboot 48.43/44.84 (the rebuild wall invisible to layout), VARS plain 100% erased, NVAR census 0 — the NVAR clock registered AMI-specific |
| `ovmf-delta-map.json` | thirty-sixth ring | the raw cartographies under the ring-34 ledgers: the 3-byte NX semantic truth amplifies 322,734x raw (9 B metadata + 968,194 B LZMA tail); the wall raw = 1,657,607 B in 26 runs — 1.63 MB LZMA tail + ~23 KB UNCOMPRESSED SECFV in-place deltas (SEC body + pointer runs shifted -9/-16, raw-visible build signal); VARS ms|snakeoil = ONE 7,309 B slab (PK/KEK/db), dbx absent |
| `findings-thirty-sixth-ring.md` | thirty-sixth ring | the cartography ring report: the compression lies (raw diff measures the container, not the content), SECFV tells the truth (any rebuild changes it, no decompression needed), the symlink second verse of the identity class, the two-tier selftest discipline |
| `findings-thirty-seventh-ring.md` | thirty-seventh ring | the promotion ring report: the ring-34 session differ (ring4_lib + ring9_differ) is now the tracked instrument fw37-differ.py — 16-gate two-tier selftest re-deriving every anchor live to the byte (identity classes, 135/144/144, the wall 126/6/120/+777472, the NX pins, the VARS census), the type-0x02 pierce lesson, UI names without census, and the day-0/release-41 linkage; no new JSON — the ring-34 registers ARE the artifact |
| `findings-thirty-eighth-ring.md` | thirty-eighth ring | the dress-rehearsal ring report: the fragmented day-0 chain becomes ONE command (fw38-exam.py) auto-filling 12/20 day-0 + 7/8 release-41 fields with the 10 manual lenses LABELED; the EXAMEN BLANC proves the oracle discriminates (OVMF stand-in profile: day-0 hits = exactly {P-04,P-05} absence, release-41 hits = ∅, wall re-derived 18/9/+777472); discoveries: OVMF has NO static DSDT (runtime-generated via QemuFwCfgAcpiPlatform), the ring-36 DSDT hit @2259668 was a checksum-false positive now refused audibly, genome transition labels carry the vendor prefix |
| `vendor-identity-matrix.json` | thirty-ninth ring | the identity calibration register: anchors for all nine rungs re-derived from their source registers (sha conventions FULL vs BODY0x40000 documented per rung), the 36-pair separation matrix (floor_full 2, floor_semantic 1 — no two known rungs ever separate by ≥3 semantic axes, so P-18's threshold is STRICTER than the adjacent-pair floor), the live self-ID table (9/9 known-releases at 0 diffs, TUF no-match at 3), the trust-cert lens validation 10/10 specimens, cross-vendor zero re-derived, the constant −4 module_count residual |
| `findings-thirty-ninth-ring.md` | thirty-ninth ring | the identity ring report: the H3 machine (fw39-identity.py) with eight scored axes and the verdict semantics wired to P-18; discoveries: the sha16 conventions are dual and derivable (FULL sentinels vs BODY0x40000 CAP-strips), the trust-cert lens born (raw-plane X.509 walk, set-equality 10/10), the armor wave hides behind a nested LZMA pierce (the shallow under-report would have manufactured a false H3 on a repack), the quartet counts by genome-flag GUIDs not UI names, DSDTs ship as four checksum-valid variants (occ-2 = the clock), P-18's ≥3 is stricter than the measured floor of 1 |
| `vendor-scorelines.json` | fortieth ring | the scoreline envelope: the FULL welded chain (no lenses) scored against the 26 frozen predictions on four rungs + the OVMF floor — the MONOTONE DECLINE 10→8→7→4 hits with chronological distance, every decline step a registered event (armor wave 3802, cert freeze 3810, AGESA/legacy/DSDT crossings 4655); the stranger signature {P-04,P-05,P-18} (stronger than the ring-38 mock invariant); the 3604\|3802 discriminators {P-04,P-05}; the release-41 null-model (pair 3802→3810: 3 hit / 2 miss) |
| `findings-fortieth-ring.md` | fortieth ring | the weld ring report: the last manual seam of the day-0 chain becomes fw40-merge.py with four laws (schema/separation/precedence/provenance); the precedence law paid for itself on the first live run (the exam shallow walk picked the wrong DSDT variant — P-09 would have missed on a byte-identical rung); the whitelist convention trap caught live (judge's day-0 41-convention would corrupt P-26 — the count is convention-DEPENDENT, exam_pair's target parameterization stands); 34 gates, tier I live |
| `vendor-novelty-ledger.json` | forty-first ring | the ZERO ledger: entries=[] BY PROOF (gates I1/I2 — zero unregistered on 3604 and 4655; I3 — the foreign stand-in cannot hide), the universe snapshot (register-derived 330/14/12/46) beside the live calibration (species universe 615, SMM universe 109, the extractor deviation), universe sources per axis, and the honesty notes — the ledger grows only when an event surprises the registers; its growth IS the chronicle of the beyond |
| `findings-forty-first-ring.md` | forty-first ring | the third-reading ring report: fw41-novelty.py completes the epistemology (predicted \| known \| NOVEL); discoveries: the 330-species trap (the genome stores TRANSITION species only — the quiet majority of 615 lives only in the corpus), the armor banner is an ENTRY born at 3802 (the register's banner method cannot see 3604), the STRUCTURAL chip-table extractor is strictly more faithful than the register (STM/Micron/Numonyx 25PF/PX Series carried by all nine rungs, dropped by the original probe — registered as deviation, register stays frozen), the AGESA scheme registered correctly (6b < 7 < 8 < A < B < Ca < E < 12), the P-02 envelope parsed from structured fields; 25 gates, tier I live |
| `vendor-dossier-register.json` | forty-second ring | the dossier envelope — the triptych calibration: the BLIND-SPOT MAP (26 predictions → detector axis: 14 set / 2 co-travel / 9 blind with reasons / 1 identity — 9 of 26 predictions are invisible to the third reading), the NINE clean dossiers (every rung profile clean, zero unregistered — the monotone decline measured on the full ladder 10→8→7→6→5→4→4→4→4 hits, the late plateau = the four constants P-01/P-09/P-17/P-23), the stranger signature re-proven through the welded chain ({P-04,P-05,P-18}, 109 unregistered), the sibling residue (TUF: no-match at 3 diffs, 5 unregistered), and the pair null as a dossier (3802→3810 clean-pair, births=0 deaths=0) — the 16/09 dump lands against measured profiles |
| `findings-forty-second-ring.md` | forty-second ring | the dossier ring report: fw42-dossier.py welds the THREE readings into ONE document (the fourth weld — the third reading ran beside the chain) and adds the coherence layer nobody had: coupling classes (aligned/confirmed/bare/blind/identity), identity couplings (known-clean/coherent-foreign/suspicious/refused-contradiction — impossible by the coverage proof), claim-free profiles (the H3 question deliberately NOT a profile); two engineering welds measured (one deep walk per image via pure memoization — gates I2/I9 count 5 walks for 5 images; the universes cache — 159.6 s once, 17.7 s per reading after); 23 gates, tier I live |
| `vendor-proposal-calibration.json` | forty-third ring | the law-L2 register: proposer-vs-register evidence rows collected through fw43.verify() ITSELF (the same code path the proposers run is what gets calibrated) — 47 rows on rungs 3604/3802/4655, 45 agree / 0 disagree / 2 notes, groups psp+ifr+cross all calibrated; a rerun that disagrees with this file is a loud L2 refusal, never a silent re-base |
| `findings-forty-third-ring.md` | forty-third ring | the propositions ring report: fw43-proposal.py removes the PASTE, not the operator (7 LENS_ONLY slots machine-drafted, operator reviews, corrections beat proposals); the calibration war's five grammar facts: the big AMI IFR modules carry NO UI sections anywhere (names from the ring-19 GUID join), varstores live per package LIST with instance-unique keys (the four AOD_SETUP rows prove the copies never merge), the EFI varstore layout is ring-20's not the modern spec's (TcgNvmeVar: vs_id +2, size +24), straddler bindings contribute NOTHING (no partial clamp), and THE ONE_OF LAW — the DEFAULT-flagged option's type width governs (the honesty note's plain 'widest' over-counted: 159/143 where the register froze 157/136); all four registered specimens reproduce EXACTLY; 38 gates, tier I live |
| `vendor-ledger-b550w2.json` | forty-fourth ring | the corrected board's register — the founder's identity correction (B450-PLUS class → TUF GAMING B550-PLUS WIFI II, TG550PW2.CAP) and its consequences: THREE GetPDBIOS ledgers (WIFI II 17 releases 0303→3645, non-WIFI 34, WI-FI v1 24 — 3644 present in the first two, absent from v1), question_3644 resolved by ACQUISITION (H2 proven / literal-H3 refuted / machine-H3 confirmed — both were right at their own level), the ring-12 'contaminated' metadata rehabilitated (Aug 2026 + AGESA 1.2.0.12 match the WIFI II 3644 desc exactly), the CAP convention doubling (B450 0x800→16 MiB, B550 0x1000→32 MiB), three vendor-verified acquisitions (3644 5f7a0e439772c160, 3645 0a1b5bda5be78e4b, sibling 3644 939f90f62de71402), the sibling same-version test (32.0% of bytes differ — a version number never identifies an image), the triptych stranger point (4 hit {P-03,P-06,P-08,P-18} / 8 miss / 14 na, profile stranger + coherent-foreign + no-match-h3-eligible), the genome transfer (596/644 species known, certs 0 unregistered), and the honesty notes (74-vs-60 unregistered accounting delta flagged, IFR lens slots loud-degraded, WI-FI v1 needs the parenthesized model string) |
| `findings-forty-fourth-ring.md` | forty-fourth ring | the corrected-board ring report: the acquisition that resolved question_3644 before the dump existed; the triptych's measured stranger point and its clean decomposition (4 board-independent hits vs 8 board-dependent misses — the first measured calibration of the oracle's cross-board reach); the nested-LZMA precedence lesson reproducing verbatim on an uncalibrated board; the 3644→3645 cartography (53 runs, 7.54 MB, 81 KB of UNCOMPRESSED code change — the byte map of 'un meilleur bios'); the version-number trap measured on the family; the pre-registered 16/09 reading (stock board ⇒ scoreline {P-03,P-06,P-08,P-18} + identity known-clean vs the acquired 3644) |
| `vendor-novelty-recon.json` | forty-fifth ring | the 74-vs-60 reconciliation — the ring-44 flagged accounting delta resolved as a KEY-NAMING asymmetry: the novelty summary counts the whitelist axis's unregistered names from its local `extra` list (74 = 48+0+4+14+8+0) while the serialized per-axis walk read only `unregistered` keys (60); fw41 UNCHANGED (its summary was the full honest count; the coherence layer already agreed at 74); the 14 named — six AUX fan-header strings + eight Level strings, real B550 fan-control vocabulary swept by the ring-41 structural extractor, NOT chip families: missing = 0, the 47-family chip whitelist transfers whole |
| `vendor-b550-lens.json` | forty-fifth ring | the B550 transfer calibration — the three vendor-verified acquisitions become the corrected board's L2 basis: PSP/IFR/cross anchor rows re-derivable live (57 rows), the transfer laws frozen as gates (release IFR IDENTITY — 3644 ≡ 3645 on all census totals and the whole cross; validity 100% transfers; share 85.73% and Setup uncovered EXACTLY 161 B on all three; PSP structure 14/221/10 with the B450 by_magic signature; set deltas 37 release vs 65 board — the version-trap ordering reproduced on the PSP axis; the PSP set DISCRIMINATES 3644 from 3645 where the IFR cannot), the day-0 lens expectation (stock 3644 ⇒ 14431/1.00/161/85.73/203/194/193), and the machine-derived lens-witnessed scoreline 6 hit / 1 partial {P-15, 161 inside its registered widened [110,174]} / 10 miss / 9 na scored through the FROZEN oracle |
| `findings-forty-fifth-ring.md` | forty-fifth ring | the B550-lens-layer ring report: the ring-44 'L3 refused' attribution corrected by MEASUREMENT (fw43.propose ran 7/7 on B550 in 5.2 s — the nulls were the missing --lenses file); the 74-vs-60 mechanism; the transfer laws; the discriminator law (PSP separates 3644/3645, IFR cannot); the P-15 widened-semantics lesson (the oracle corrected the ring's manual reading); the doubly pre-registered 16/09 reading (4/8/14 without lenses, 6/1/10/9 with) |
| `vendor-autodossier-register.json` | forty-sixth ring | the auto-dossier register — the day-0 chain fused into ONE command and MEASURED: the crown fused dossier on b550w2-3644.rom (6 hit {P-03,P-06,P-08,P-13,P-16,P-18} / 1 partial {P-15} / 10 miss / 9 na in 3m42.8s — the ring-45 DERIVED scoreline now MEASURED end-to-end, agree; the ring-44 stranger point reproduced WITH lenses), the PSP SET anchors (203/194/193 sha256_16 captured from the crown proposals) that fix the count-blindness find, and the discriminator 3645 registered BOTH ways (set != 37/37 while count == 203 — a count anchor would have certified a flashed-3645 board as stock) |
| `vendor-pairmarks-register.json` | forty-seventh ring | the pair-witness register — the pair coherence anchored to the SETS and MEASURED: the release-pair crown (3644→3645: PSP != 37/37 vs the lens anchor, count witness == at 203/203, species 0/0, 18 modules changed −992 B, fw42 clean-pair CORRECTED to swap-event with the correction named, fw42 side-readings identity no-match-h3-eligible + novelty 75 unregistered vs 74 on 3644 — +1 recorded), the board pair (3644→nw-3644: 65/65, 0 births / 4 DEATHS, 108 changed −13,008 B — registered-births; PR-6 refuted, kept visible), the null3802 verdict (the ring-42 frozen clean-pair was set-blind: 17/17 inside 193-hash sets, 34 modules −86,264 B — verdict corrected, the frozen register never rewritten), the corrected pair taxonomy (clean-pair REQUIRES set delta == 0; swap-event = the new class), the 8-PR pre-registration ledger, and the day-1 post-flash usage |
| `findings-forty-sixth-ring.md` | forty-sixth ring | the auto-dossier ring report: the last manual surface was the CHAIN itself (three commands, one memory — the weld-class failure one layer higher); the count-blindness find (the ring-45 review marks compared PSP sets by COUNT and could not see the 37/37 swap inside a 203-hash set — measured live on 3645 before the crown); the crown (the derived 6/1/10/9 measured through the fused chain, agree); the four laws (corrections-beat-proposals journaled, calibration-before-fusion, loud degradation, zero writes); the day-0 protocol as ONE command |
| `findings-forty-seventh-ring.md` | forty-seventh ring | the pair witness ring report: the pair layer was blind to the law the registers carry (fw42's dossier_pair reads SPECIES only — the 3644→3645 release kills zero species and reads clean-pair on THE discriminator pair); the crown (the release pair measured 206 s: PSP != 37/37 anchor-checked, count witness ==, 0/0 species, 18 modules changed −992 B, CORRECTION NAMED clean-pair → swap-event; the board pair 65/65, 0 births / 4 DEATHS, 108 changed — registered-births: the version-number trap gets its pair-level signature); the null3802 verdict (the ring-42 frozen null was itself count-blind: 17/17 PSP hashes inside 193-hash sets, 34 modules −86,264 B under a frozen clean-pair — corrected, never rewritten); the pre-registration ledger (8 PRs, PR-6 refuted by measurement, kept visible) |
| `vendor-pairaxes-register.json` | forty-eighth ring | the full-axis pair-witness register — the set lens extended to the five non-PSP content axes (certs / dsdt / whitelist / smm / agesa), MEASURED on three pairs: the release pair (3644→3645: psp 37/37 reproduced, CERTS +1/−1 — one DER certificate rotated 2da09da7f4131f9f → 4aa03fa22cd75c84 inside a stable count of 6, the count-blindness class caught on a NEW axis; dsdt/whitelist/smm/agesa all ==; signature multi-axis-swap), the board pair (3644→nw-3644: DSDT +4/−4 — FOUR entirely different variants, zero overlap, at stable count 4, the WIFI II's primary bdd18b8af25e0c7a SKU-exclusive; certs/whitelist/smm/agesa all ==; signature registered-births), the B450 null re-read (3802→3810: certs +1/−3 — the trust store rebuilt 4→6, the corpus's freeze point pair-explained; exploratory, no anchor); THE AXIS MAP (certs is a RELEASE marker, dsdt is a BOARD marker — orthogonal signatures); the 8-PR ledger (5 hit / 3 refuted, all visible: PR-2 the cert rotation, PR-7 the fan vocabulary is B550-generation not WIFI-II-specific, PR-8 not psp-localized); the dump law (the cert set alone discriminates stock-3644 from flashed-3645, count-blind-proof; the DSDT set alone certifies the board identity) |
| `findings-forty-eighth-ring.md` | forty-eighth ring | the full-axis ring report: the axis map measured on three pairs (the release rotates ONE certificate under a stable count of 6 — the blindness class on a new axis; the same-version board swaps ALL FOUR DSDT variants under a stable count of 4; the B450 3810 rebuilds the trust store 4→6 — the corpus freeze point pair-explained); the ORTHOGONALITY LAW (certs is a release marker, dsdt is a board marker — neither count-level mark could see either); the 8-PR ledger 5 hit / 3 refuted, each refutation more informative refuted (the cert rotation, the B550-generation fan vocabulary, no measured pair instantiates psp-localized-swap); the dump law for 16/09 |
| `findings-forty-ninth-ring.md` | forty-ninth ring | the ReBAR axis report: the founder's "le reshade bar ... il détecte mal alors que c'est en auto" measured to the byte — the vocabulary law ("Resizable" is absent from ~48.6 MB decompressed ×3; the ASUS label is "Resize BAR Support", the word drops the "able"), the name-table decode (ResizeBarSupport @Setup 0x1BB between Above4gDecode 0x1BA and SriovSupport 0x1BC, SMM driver 64BEA199), the axis triptych (ONE_OF qid 0x20F tok 1403, options {0x00,0x01}, SUPPRESS_IF Above4gDecode==0 + GRAY_OUT_IF qid 0x361==1, the vendor help documenting the CSM condition verbatim), the pair law (release 3644→3645 rebar-axis-INVARIANT; board pair invariant MODULO one byte — the grayout qid 0x361→0x35F), and the day-0/day-1 chain closing the diag-gpu's "not observable from the OS" gap |
| `vendor-rebarfact-register.json` | fifty-first ring | the ReBAR-factory crown — what the vendor actually ships: the B550 NVAR stores first-parsed (two windows @0x40078/0x1040078, outer = StdDefaults only, 17 clean nested entries, inner Setup 515/514 B matching the ring-50 IFR census), the five-gate factory canon (Mmio 0x27 / Above4g **0x00 Disabled** / **ResizeBarSupport 0x00 Disabled** / Sriov 0x00 / Csm 0x00 — the shipped chain dead-on-arrival and coherent: Above4g-off hides the ReBAR question at factory), the two-source cross (15/15 agree; ring 49's raw 5B-06 ops RESOLVED as standard EFI_IFR_DEFAULT, ids 0 and 1, both 0x00), the factory pair laws (release byte-invariant; board invariant modulo the tail span [513,514]; B450 3604 ships the SAME disabled chain — generation-wide), the 9-PR ledger (7 hit / 2 REFUTED pre-named), and the day-0 five-gate byte court with reversed live expectations (live Above4g/ReBAR expected 0x01 — the written state the dump must confirm) |
| `vendor-rebarlang-register.json` | fiftieth ring | the ReBAR-language crown — the option labels decoded at the strings-package floor: the standard EFI_HII_PACKAGE_HEADER (367586, Length 0x041DC2, Type 0x04, HdrSize 0x34, LanguageWindow all-zero, LanguageName 1, tag en-US), the id-less-block base law (tok = ordinal + 1, verified by the 1280-step forward count to the tok-1281 anchor both ways), toks 1-8 (English/''/Enabled/Disabled/Auto/Setup/Setup/Main), the decoding-family vocabularies (Above4gDecode/SriovSupport/CsmSupport = {Disabled, Enabled}; **ResizeBarSupport = {0x00: Disabled, 0x01: Auto} — no Enabled, the founder's Auto byte-true**), the grayout operand NAMED (qid 0x361 = hidden NUMERIC u8 @varstore 4 offset 0 = **SystemAccess**, 1 B, GUID E770BB69-BCB4-4D04-9E97-23FF9456FEAC — the option grays out for restricted sessions), vsid rows 1-51 (48 = SecureVarPresent, 6 B — the two 'extra WIFI questions' refuted as Secure-Boot presence readers), the pair law at the language level (invariant across all three acquisitions, mover = the grayout qid), the 8-PR ledger (8 hit, PR-6/PR-8 carrying ring-49's refutations), and the day-0 Auto-chain checklist |
| `findings-fifty-first-ring.md` | fifty-first ring | the factory-law ring report: the B550 factory store's first parse (two NVAR windows, one outer StdDefaults variable each, 17 clean nested entries, the B550 factory grammar named, SystemAccess absent — the grayout gate ships open), the factory law (the five gates read from the StdDefaults Setup blob and crossed with the IFR defaults: **the vendor ships the whole decode chain OFF — ReBAR 0x00 Disabled @0x1BB, Above4G 0x00, CSM 0x00, 15/15 source crosses agree, layout A derived not assumed**), the founder's word re-read ("c'est en auto" is a WRITTEN state, not the factory state — his board's live chain was engaged by someone/something the dump will name), the factory-level pair laws (release-invariant with zero diff spans; board-invariant modulo the tail span [513,514]; the B450 3604 null ships the SAME disabled chain — the law is generation-wide), the 9-PR ledger (7 hit / 2 REFUTED, both refutations pre-named as the informative outcomes), and the five-gate byte court for the 16/09 dump |
| `findings-fiftieth-ring.md` | fiftieth ring | the ReBAR-language ring report: the correction front (ring 49's SCSU reading refuted — the labels are plain UTF-16; the failure was its own backward walker, 50 mis-steps/1280 measured, empty entries + the unvalidated anchor_table dive into machine code), the vocabulary front (the founder byte-true: Auto is the ONLY positive value; no Enabled exists), the grayout front (SystemAccess named), the SecureVarPresent correction front, the language-level pair law front, the 8-PR ledger, the five-gate day-0 checklist |

### The instruments

| Path | What it is |
|---|---|
| `fw37-differ.py` | the GUID-aligned module differ, TRACKED (ring 37) — promoted from the session scripts that built the ring-34 registers: `scan` (FV census + generic LZMA pierce + FFS walk, UI names without census), `ledger` (the ring-34 §2 shape, release-41 P-20..P-26 input), `pin` (exact byte-run pins, the CAP comparator §3 layer), `vars` (the 60-byte-header auth walk + pairwise delta); two-tier selftest, 16 gates, tier I live on the surviving corpus — exits 2 on any drift |
| `fw38-exam.py` | the conductor + the dress rehearsal, TRACKED (ring 38) — imports fw36/fw37/fw35 as modules and fuses eight registers: `exam <image>` (the full measurable chain → findings draft → fw35 score, 12/20 day-0 fields auto-filled, manual lenses labeled), `exam-pair` (the release-41 lens: 7/8 fields, GUID-truth births/deaths), `mock` (the EXAMEN BLANC: discrimination invariant — on a non-target image only absence-predictions may hit), `selftest` (two-tier, 20 gates, tier I live); new in-ring: checksum-validated ACPI walk superseding the ring-36 length-plausibility heuristic with audible refusals |
| `fw39-identity.py` | the identity engine (the H3 machine), TRACKED (ring 39) — fuses nine registers to judge `is this image one of the known releases?`: eight scored axes (rom_sha under BOTH measured conventions, AGESA, DSDT 4-variant set, SMM census, trust certs, armor core/added by GUID, genome profile), report-only axes (module_count, whitelist strings, PSP), recursive deep pierce reaching the nested armor wave the shallow walk misses, `identify` verdicts wired to fw35 P-18 (known-release / no-match-h3-eligible ≥3 / no-match-below-floor 1-2), `matrix` (the 36-pair calibration), `selftest` (two-tier, 54 gates, tier I live); the trust-cert lens born and validated 10/10 specimens |
| `fw40-merge.py` | the weld, TRACKED (ring 40) — the last manual seam of the day-0 chain (merge exam+identify before score) becomes a machine-checked instrument with four laws (schema / separation / precedence / provenance), `merge` (drafts + lenses → validated findings), `chain` (the day-0 ONE command: exam+identify+weld+score), `chain-pair` (the release-41 ONE command: exam_pair+identify+weld+score), `ceiling` (the scoreline envelope on known rungs), `selftest` (two-tier, 34 gates, tier I live); the precedence law (judge deep-truth > exam shallow-walk, lens only via `--lens-wins`) caught the DSDT-variant conflict live on 3604 and the whitelist convention trap live on the pair null-model |
| `fw41-novelty.py` | the unregistered detector (the third reading), TRACKED (ring 41) — completes the epistemology `predicted (fw35) \| known (fw39) \| NOVEL (fw41)`: six axes (species 330+live-union=615, certs union 14, DSDT union 12, AGESA ladder+P-02 envelope+P-20 floor, whitelist 46+deviation via the STRUCTURAL chip-table extractor, SMM live union 109), classes known/anticipated/unregistered with loud claim-free degradation, `novelty <image>`, `ledger`, `selftest` (two-tier, 25 gates, tier I live), `manifest`; the needle gate (one flipped GUID byte → exactly one unregistered species), the extractor gate (register covered 9/9, deviation stable — the instrument beat the register by one chip name), the stranger reading (OVMF 109 unregistered) |
| `fw42-dossier.py` | the triptych machine (the fourth weld), TRACKED (ring 42) — welds the THREE readings into ONE dossier (`dossier <image>`: the day-0 ONE command, lenses pass through; `dossier-pair <old> <new>`: the release-41 command with the brand-new-species class — births beyond the register universe) and adds the coherence layer: the blind-spot map (26 rows, 9 blind with reasons), coupling classes, identity couplings (contradiction = exit 2, not a class), claim-free profiles; four laws (L1 completeness / L2 coherence / L3 calibration — byte-identity forces the scoreline onto the registered ceiling / L4 separation echoed from the weld); two pure performance welds (one deep walk per image, one universes build per process — 9× measured); `clean <rung>`, `register`, `selftest` (two-tier, 23 gates, tier I live), `manifest` |
| `fw43-proposal.py` | the propositions (the last manual surface meets the machine), TRACKED (ring 43) — machine-drafts the seven LENS_ONLY slots the operator used to paste: PSP proposer (registered layout, fletcher32-validated dirs, one L2 hop, sha256_16 body-hash SETs for the dump + the 3604/3802 rungs) and IFR proposer + varstore cross (ring-6 exact-consumption walk + ring-20 coverage at ring-19 scope over the fw39 deep walk, memoized); four laws (L1 proposal-not-measurement / L2 calibration-before-proposal against `vendor-proposal-calibration.json` / L3 loud degradation — omitted slots carry reasons, never guesses / L4 zero writes, grep-policed); `propose <image> [--out]` (review table + sidecar report), `verify <rung>`, `selftest` (two-tier, 38 gates, tier I live), `manifest` — the judge-owned identity slots are NEVER proposed |
| `fw45-b550-lens.py` | the B550 lens layer (transfer calibration + anchored proposals + the reconciliation), TRACKED (ring 45) — gives the corrected board its own L2 basis: `propose <image> [--out]` re-derives every B550 anchor row (57) + the shared B450 authorship rows (10) BEFORE proposing (L2-transfer: a broken basis proposes nothing), the review table marks each slot `== stock-3644 anchor` or `!=` with the anchor beside it, the sidecar report carries the day-0 expectation; `verify` (all anchor rows re-derived, exit 2 on any drift), `recon` (the 74-vs-60 reconciliation live on the corrected board), `selftest` (two-tier, 28 gates, tier I live — anchors, delta laws 37/65, cross-family 40/40, shared-path agreement, the weld consuming 7/7 proposals as journaled lenses, the reconciliation reproducing live), `manifest`; L1/L3/L4 inherited from fw43, the Refusal-classes-are-per-module boundary lesson banked in the gate code |
| `fw46-autodossier.py` | the auto-dossier (the day-0 chain fused into ONE command), TRACKED (ring 46) — `fused <image> [--target day0|release41] [--lenses corrections.json] [--review-only] [--out]`: fw45.propose's L2-transfer gates run FIRST (L2 calibration-before-fusion), then `marks_for` marks every slot against the stock-3644 anchor (three kinds: SET anchors with added/removed deltas — the 203/194/193 registered strings; the ring-45 count fallback loudly labelled set-blind; scalars; unknown slots `no-anchor`, never invented), then `compose` applies L1 (corrections beat proposals, journaled operator_correction vs proposal; corrections outside LENS_ONLY or on judge-owned slots refuse with the true violation named), then the fw42 triptych dossier unchanged — the whole provenance chain (proposal → mark → journal → dossier) in ONE artifact; `selftest` (two-tier, 38 gates, tier I live — the discriminator firing BOTH ways on 3645: set != 37/37 while count == 203; live compose through the real weld; THE CROWN: the full fused stock-3644 dossier re-run, 6/1/10/9 + the stranger point confronted to the register; artifact roundtrip), `manifest`; L4 zero-writes grep-policed with the self-matching-pattern lesson banked (the ring-43 R9 lesson bitten again) |
| `fw47-pairmarks.py` | the pair witness (set-anchored pair coherence), TRACKED (ring 47) — `marks <old> <new> [--out]`: the cheap pair review (live PSP body-hash sets on both images via fw43.psp_bodies on fw37.load, SET marks vs the lens register's psp_set_deltas with the anchor checked BOTH directions or a loud refusal, counts emitted ONLY as the labeled blindness witness, species births/deaths + changed-ledger row via fw37.Engine.ledger memoized per process, the corrected class, the blindness witness); `cohere <old> <new> [--out]`: fw42.dossier_pair composed with the marks (Refusal classes per-module re-claimed, the ring-42 lesson) — the corrected pair dossier with the profile_correction NAMED when the frozen class and the corrected class disagree; `null3802 [--out]`: the frozen pair-null re-read (births/deaths must reproduce the frozen row or refuse; no anchor claimed for the unregistered B450 sets; verdict survives|corrected); the corrected taxonomy — refused > beyond-register-births > registered-births > swap-event (NEW: content swapped inside stable containers) > clean-pair (now REQUIRES set delta == 0); `selftest` (two-tier, 32 gates, tier I live — the release pair 37/37 with count == live, the board pair 65/65 with 0/4 species, the null re-read live, the crown register cross-checked when present); `manifest`; L1 set-truth-over-count / L2 anchors-before-marks (numbers live in registers, never code) / L3 corrected-taxonomy (frozen registers read, never rewritten) / L4 zero writes (one write site, assembled-pattern police) |
| `fw48-pairaxes.py` | the full-axis pair witness (the set lens on every axis), TRACKED (ring 48) — `axes <image> [--out]`: the live axis sets of ONE image (certs via fw39.trust_census, dsdt via fw38.x_acpi checksum-valid sha16s, whitelist via fw41._armor_names, smm via the SMM-typed GUIDs of fw41's deep walk, agesa via fw38.x_agesa — memoized per abspath, items carried whole); `marks <old> <new> [--out]`: fw47.pair_marks composed (PSP + species, never re-implemented, refusals re-claimed per-module) + the five axis marks (anchor-checked against this ring's crown register when present — a registered pair+axis must reproduce or refuse; unknown pairs and the exploratory null claim nothing, L2) + per-axis classes + THE SIGNATURE from the full evidence (registered-births > psp-localized-swap > multi-axis-swap > clean-pair — the sharpened taxonomy) + the multi-axis blindness witness (axes swapped inside stable counts); `null3802 [--out]`: the B450 founder pair re-read on ALL axes (fw47's frozen-row reproduction first; the non-PSP rows exploratory, no anchor); `selftest` (two-tier, 32 gates, tier I live — the axes re-derive on the acquisitions, both pairs marked full-axis, the corollary law live (species 0/0 ⇒ smm ==), the crown confronted to a live re-derivation); `manifest` with the 8-PR ledger printed; L1 set-truth-over-count (per axis) / L2 anchors-before-marks (crown-anchored, basis stated where the anchors live) / L3 corrected-taxonomy (the signature reads the FULL evidence; contradictions named, never smoothed) / L4 zero writes (one write site, grep-policed) |
| `fw49-rebar.py` | the ReBAR axis (the founder's mis-detected Auto, made observable), TRACKED (ring 49) — `axis <image>`: the full truth chain (name-table fields via the SMM driver 64BEA199, the axis questions via fw43.direct_forms, the label anchor tok 1281 verified both ways, the ONE_OF options + AMI 0x5B-06 value ops, the SUPPRESS_IF/GRAY_OUT predicates, the help text); `pair <old> <new>`: the axis-region byte identity (release invariant; board invariant modulo the grayout qid); `nullrebar`: the OS-side null (what diag-gpu renders); `selftest` (two-tier, 11 gates), `manifest`; L1 labels-over-guesses / L2 anchors-before-marks / L3 zero writes / L4 compose-on-frozen |
| `fw50-rebarlang.py` | the ReBAR language (what the options actually say), TRACKED (ring 50) — `lang <image>`: the strings-package header found by pattern + validated by the 1280-step forward count (L1 both ways), the forward-map base law (tok = ordinal + 1), toks 1-8, the four sisters' ONE_OF vocabularies with labels, the grayout operand's full identity (op/vsid/voff/varstore), the vsid rows 1-51; `pair <old> <new>`: the language-level pair law; `nulllang`: the ring-49 walker's mis-step census (50/1280, empty entries — the honest measurement of a flaw); `selftest` (two-tier, 15 gates — R3 reproduces the entry_before skip on demand), `manifest` with the 8-PR ledger; L1 labels-over-guesses (forward map from a MEASURED floor) / L2 anchors-before-marks (the sisters reproduce the ring-49 crown or refuse) / L3 zero writes / L4 compose-on-frozen (entry_before only where proven) |
| `fw51-rebarfact.py` | the factory law of the ReBAR chain (what the vendor actually ships), TRACKED (ring 51) — `fact <image>`: both default sources for one acquisition (the NVAR StdDefaults factory canon via the ring-14 grammar carried verbatim — first parse of the B550 acquisitions — and the IFR option flags + 0x5B DEFAULT statements via fw49's construct walk, the layout DERIVED per question by the uniqueness+membership chooser, ambiguous/raw-only unclaimed) + the per-gate cross; `pair <old> <new>`: the factory pair law (blob shas, diff spans, gate hits); `null450 <image>`: the generational null under board-relative name-table voffs (0x0 metas reported, never claimed; no crown comparison); `day0`: the five-gate byte court + live-minus-factory protocol + failure lexicon; `selftest` (two-tier, 13 gates incl. the crown bridge), `manifest` with the 9-PR ledger; L1 bytes-over-guesses / L2 anchors-before-marks (B550 crown scope; the B450 null runs its own constructs) / L3 zero writes / L4 compose-on-frozen (fw49 imported; ring-14 grammar pinned by KATs and store-shape anchors) |
| `ovmf-smoke.sh` | boot OVMF in QEMU, headless, capture the boot log — the first "replacing the firmware" experiment, fully reversible by Ctrl-C |
| `coreboot-notes.md` | the Volume 5 doctrine: the sacrificial board, dump-first, programmer, candidate machines |

The one-shot probe scripts (`*_probe.py`, `ring5_probe.py`, `ring4_lib`) are
**session instruments of the sandbox, deliberately not tracked here** —
they read the downloaded OVMF images from sandbox paths and print
`bytes written: 0`; the findings quote them, the JSON artifacts are
their durable output. The repo tracks knowledge, not scratch. (Ring 37
is the standing exception that proves the rule: the differ walkers
behind the ring-34 registers were promoted into the tracked
`fw37-differ.py` because both remaining measurement events consume
them — the probes themselves stay sandbox-side.)

## Why a lab at all (the honest answer)

Replacing the vendor BIOS unlocks things the runtime can never see: a
readable SMM, a sovereign boot policy, a neutralizable ME. It also
destroys the object of study — every Omarchy user out there runs vendor
firmware, and the guard exists for *their* machines. The lab is where
both truths coexist: here we replace, we instrument, we break; in the
guard fleet we read, we explain, and we write zero bytes.
