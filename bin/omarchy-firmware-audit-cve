#!/usr/bin/env python3
# omarchy:summary=Cross-check BIOS version against known AM4 CVEs (LogoFAIL, Sinkclose...)
# omarchy:args=[--json]
# omarchy:examples=omarchy firmware audit cve

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.realpath(__file__))), "lib"))
from firmware_hal.cli import main  # noqa: E402

sys.exit(main(["audit", "cve", *sys.argv[1:]]))
