#!/usr/bin/env python3
# omarchy:summary=Show the T0 access journal (every firmware read is logged)
# omarchy:args=[N] [--json]
# omarchy:examples=omarchy firmware journal 20

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.realpath(__file__))), "lib"))
from firmware_hal.cli import main  # noqa: E402

sys.exit(main(["journal", *sys.argv[1:]]))
