#!/usr/bin/env python3
# omarchy:summary=Full firmware inventory: board, BIOS, boot chain, fwupd, sensors
# omarchy:args=[--json]
# omarchy:examples=omarchy firmware audit status

import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.realpath(__file__))), "lib"))
from firmware_hal.cli import main  # noqa: E402

sys.exit(main(["audit", "status", *sys.argv[1:]]))
