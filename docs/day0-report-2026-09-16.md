# Day-0 report — ASUS PRIME B450-PLUS live dump (2026-09-16)

Status: **template**. Every table is filled on the day, from the
measured registers, with the frozen instruments. Nothing here is a
prediction; expectations are cited only so the measurement can agree
or disagree with them. Fill order follows `docs/day0-protocol.md`.
The +0-octet rule governs everything: the dump lands on the offline
drive, the SPI is never written, no flash, no daemon — the update
decision belongs to the official ASUS tools, and this report only
sharpens it.

## 0. Acquisition record

| field | value |
|---|---|
| date/time (UTC) | |
| board | ASUS PRIME B450-PLUS |
| programmer / tool | |
| dump size | |
| sha256 (full) | |
| +0 octet check (SPI writes) | |
| host OS | |

Sanity gate before anything else: the dump size decides which lenses
run. 16 MiB → single-window path (all six lenses). 32 MiB or paired
structures → also run the combo lens (see §5).

## 1. Identity triangulation — three independent paths

Fill each path *before* looking at the others; disagreement between
paths is itself a finding.

### 1.1 AGESA level (ring-16 lens)

| signal | measured | nearest known |
|---|---|---|
| `ComboAM4v2PI` string | | nine dated rungs on this board (ring 18): 1.2.0.6b=3604 · 1.2.0.7=3802/3810 · 1.2.0.8=4003 · 1.2.0.A=4202 · 1.2.0.B=4402 · 1.2.0.Ca=4604 · 1.2.0.E=4631 · 1.2.0.12=4655 — a level between two rungs dates the build between their releases |
| pierced-surface count | | raw scan is not enough — the strings sit 2.7–2.9 MiB deep; ladder lives in `vendor-agesa.json` (timeline) |

### 1.2 PSP fingerprints (ring-13 lens)

| dir | magic | count | fletcher32 | sha256_16 | nearest known release |
|---|---|---|---|---|---|
| | | | | | |
| | | | | | |

Compare against `vendor-psp.json` (the five known specimens). The
ring-13 cross-release register (37 identical blobs, 38 changed
between 3604 and 4655) is the dictionary.

### 1.3 NVRAM factory fingerprint (ring-14/16 lens)

| signal | measured |
|---|---|
| NVAR anchor store size | (3604 and 4655 both measure 130,952 B — size alone decides nothing) |
| anchor content sha | |
| HWM/QFan region vs 4655 | (ring 16: their 778-B diff sits in `NV_SIO0_LD1` / `SetupHWMOneof` / `QFan` spans) |
| `Setup` blob size + sha | (factory: 456 B on ASUS) |
| shared variable names | (expect the 9-name canon: AMITSESetup, NetworkStackVar, PCI_COMMON, PlatformLang, SecureBootSetup, Setup, Timeout, UsbSupport, XhciDID) |

### 1.4 Identity verdict

| box | meaning |
|---|---|
| [ ] | equals 4655 (2026-08-27) |
| [ ] | equals 3604 (2022-03-16) |
| [ ] | equals another known release (name: ) |
| [ ] | matches no known release — H3 survives, the 3644 question reopens with three new inputs (§2) |

### 1.5 Ring-19 fingerprints — the fourth signals (fill alongside §1.1-1.3)

Ring 19 added independent first-hour signals measured on the ten
on-disk ASUS rungs; all expect a clean match here.

| signal | measured | expectation from the registers |
|---|---|---|
| Authenticode DER set (raw scan) | | SIGHTINGS, not distinct certs: 18 expected (12 if pre-3810) = each trust certificate shipped 3× across the PK/KEK/db store copies; DISTINCT bodies: 4 if pre-3810, 6 from 3810 (the register's "12→18" collapses — `vendor-der-inventory.json`). Set = ASUS leaf (Private Organization TW 23638777) + DigiCert G4 Code Signing 2021 CA1 + the rotating timestamp chain; the timestamp responder DATES the build: 2021=3604 · 2022-2=3802/3810 · 2023=4402/4604 · 2024+leaf-renewal=4631 · 2025 responder=4645/4655; leaf validity window 2024-05-27→2027-05-30 on a 4631+ build is itself a dating read |
| geometry (64 KiB entropy atlas) | | FV share 41-42 % of 16 MiB, packed 56-60 %, erased 18-19 %, top 0xFF slack 1.05-1.09 MiB, NVAR magic 18-19 (`vendor-geometry.json`); deviation = pre-parse anomaly flag. **Instrument: `lab/fw36-atlas.py atlas` computes this fingerprint in seconds on the dump** (ring 36). Two ring-36 boundaries: raw diffs of compressed regions measure the LZMA container, not the content (the 3-byte NX truth amplifies 322,734x raw on OVMF) — read SECFV raw for a build-difference signal, module-walk FVMAIN for semantics; and NVAR is AMI-specific (census 0 on OVMF's auth-variable store) |
| armor anatomy (for §3.1) | | `SbRomArmorSmm` one byte-identical body since 3802; `PrepareWhiteListSmm` carries `AMD rom armor` + SPI chip DB (ADESTO/AMIC/ATMEL...), 3 bodies across rungs; SMM census expected 107-109 (`vendor-lifecycles.json`) |
| facade magnitude (for §6) | | ~7,975-8,273 questions / 779-783 pages across ~15 modules on this board; ~zero SUPPRESS/GRAYOUT/DISABLE conditions; 100 % exact-consumption validity expected (`vendor-ifr-census.json`) |
| five-clock decision tree (ring 20) | | the dating clocks fuse cost-ordered: (1) geometry — seconds, raw read, expect FV 41-42 % / packed 56-60 % / slack ~1.06 MiB, deviation = anomaly flag; (2) cert set — raw scan, no pierce: 12 DERs = pre-3810, 18 = 3810+, timestamp year dates the build — the ONLY clock separating 3802 from 3810; (3) AGESA strings — pins the rung except the quiet pair; (4) armor census — 0/7 pre-3802, 5/7 from 3802, 7/7 from 4604, legacy 2/2 if ≤ 4003; (5) PSP churn vs neighbours. `(cert_count, AGESA)` alone resolves all nine rungs (`vendor-clocks.json`) |
| microcode clock (ring 21) | | psptool `0x66` scan — raw directory read, seconds, no pierce: expect EXACTLY 19 distinct patches (the carrier constant; any other count is a finding), newest patch date 2024-08-22 for a 4631+/1.2.0.E-era build (older year-buckets map at `vendor-microcode.json`); the 19-patch set corroborates the rung alongside `(cert_count, AGESA)` — the quiet pair 3802|3810 is NOT split by this clock either |
| DSDT clock (ring 21) | | checksum-admitted DSDT body hash against the three-generation atlas: `27d5e826` (3604-4202, 47,184 B) / `f23c571b` (4402-4604, 45,043 B) / `0a4a6f16` (4631-4655, 45,069 B); creator INTL 2014-09-25 expected on every AML body; a FOURTH body hash is a finding (`vendor-acpi.json`) |
| SSDT set clock (ring 22) | | the eighth clock, independent of the DSDT's frontiers: expect the 35-body AML atlas (4 DSDT variants + 23 RAW-SEC SSDTs + 8 EMBEDDED ALIBs) with the SSDT SET matching the three-set atlas (3604 | 3802-3810 | 4003-4655) and the 6 ALIB bodies byte-identical corpus-wide — any deviation is a finding (`vendor-ssdt-clock.json`) |
| armor chip whitelist (ring 22) | | the `AMD rom armor` banner's chip-name table: expect the SAME 46-family set byte-for-byte (frozen 3802→4655, identical on all five vendors); a different family count or set is a corpus first — 3604-era builds carry the 41-family predecessor instead (`vendor-armor-chipdb.json`) |
| ACPI shipped-vs-built (ring 21) | | expect DSDT variants + SSDTs shipped as checksum-valid raw sections inside the compressed FV, and ZERO FACP/APIC/MCFG/HPET/IVRS bytes anywhere in the image (built at runtime by the AMI ACPI layer) — finding any static-table byte would be a corpus first (`vendor-acpi.json`) |
| Setup-blob unasked bytes (ring 20) | | the `Setup` varstore on this board: 456 B declared, ~142 B never referenced by any question (83.4 % of all varstore bytes unasked), expected as 27 merged ranges (largest 28 B @ +49) at offset level (`vendor-hidden-map.json`); IFR-side size must match the NVRAM-side blob (ring 14) — a mismatch is a finding (`vendor-unasked.json`) |
| module roster + word clock (ring 23) | | two first-hour reads, zero pierce needed beyond the standard FV/FFS walk: (a) MODULES — expect ~613 FFS files with the armor quintet present (`755877A6`/`6C289241`/`391626DB`/`51080191` + freeform `89BE47F4`, born 3802) and freeform `02076249` + `4EB43107` (born 4604), the legacy pair `827E45A4`/`21782819` ABSENT on any ≥4402 build (`vendor-churn-atlas.json`); (b) WORDS — the HII string layer decodes to ~5K distinct strings and MUST contain `PSP RPMC Switch` (born 3802, the armor release's only new words) and, on a 4631+-lineage build, `Stability Boost` + `PCI Express Native Power Management` (`vendor-vocabulary-clock.json`) — any of these absent/present at the wrong rung is a finding |

## 2. The 3644 decision tree

> **Ring-35 upgrade (the oracle)**: this dump now runs against a frozen
> register of 19 quantitative expectations (`lab/vendor-oracle.json`,
> target `day0-3644`, scored by `lab/fw35-oracle.py score`). Fill
> `fw35-oracle.py template` alongside §1-§3 — the lenses below produce
> exactly the observed values the register wants — then score: every
> hit confirms a law, every partial/miss localizes which law failed to
> extend, and each requires a written cause in the next ring report.
> Registered highlights: AGESA ∈ {1.2.0.6b, 1.2.0.6c, 1.2.0.7} (point
> 1.2.0.6c), armor quartet ABSENT, 2 legacy SMM modules present, DSDT
> `27d5e826e111d755`, 4 trust certs, facade 7,900-8,050 questions,
> Setup varstore 456 B with 142 never-asked, SMM census 106-109, and
> identity = H3 with ≥ 3 independent fingerprint differences (P-18).

> **Ring-37 upgrade (the differ, tracked)**: the §3 module walk, byte
> pins and store read are now one tracked instrument —
> `lab/fw37-differ.py` (16-gate two-tier selftest, tier I live on the
> OVMF corpus: the ring-34 anchors re-derived to the byte). Day-0 use:
> `fw37-differ.py scan dump.rom` (first-hour inventory, UI names with
> no census), `fw37-differ.py ledger 3644.rom dump.rom` (the GUID-
> aligned ledger — the P-18 fingerprint generator),
> `fw37-differ.py pin ...` (byte-exact localization inside changed
> modules), `fw37-differ.py vars dump.rom` (the store lens). The same
> command path serves release-41 (`ledger release40.rom release41.rom`
> = the P-20..P-26 measurement input).

> **Ring-38 upgrade (l'examen blanc)**: the whole chain is now ONE
> command — `python3 lab/fw38-exam.py exam dump.rom --out
> findings-draft.json` runs geometry (fw36), inventory + pierce +
> vars (fw37), the AGESA ladder, armor names/GUIDs, SMM census, legacy
> SMM, the checksum-validated ACPI walk, the PSP candidate scan, the
> chip-whitelist match, assembles the fw35 findings draft (12 of the
> 20 day-0 fields auto-filled; the 10 manual lenses stay null and are
> LABELED), and scores the draft. The dress rehearsal (`fw38-exam.py
> mock`) proved on the OVMF stand-in that the oracle DISCRIMINATES:
> the only day-0 hits on a non-target image are the absence-predictions
> P-04/P-05, release-41 hits are ∅, and the rebuild wall re-derives at
> genome level (18 births / 9 deaths / +777,472 B). Ring-36 correction:
> its DSDT "hit" @2259668 was a length-plausibility false positive —
> the new walk validates checksums and refuses audibly (OVMF has NO
> static DSDT: tables are runtime-generated via QemuFwCfgAcpiPlatform).

> **Ring-39 upgrade (le moteur d'identité)**: the H3 question gets its
> judge — `python3 lab/fw39-identity.py identify dump.rom` compares the
> dump against ALL nine registered releases on eight scored axes
> (rom-sha under both measured conventions, AGESA, the four DSDT
> variants, SMM census, trust certs, armor by GUID, genome profile) and
> returns a verdict wired to P-18: `known-release` (byte-identity),
> `no-match-h3-eligible` (≥3 independent fingerprints from EVERY known
> release — the H3 shape), or `no-match-below-floor` (1-2 — a repack or
> neighbour). Calibrated live on the corpus: all nine releases
> self-identify at 0 diffs, TUF 4645 no-matches at 3, and the separation
> floor between known rungs is 1 semantic axis — so clearing 3 means
> MORE distinct than any pair in the chronology. It also fills
> `trust_cert_distinct` (P-11) and carries deep-walk overrides for the
> armor/legacy/whitelist lenses (the armor wave lives behind a nested
> LZMA section — merge the identify output into the exam draft before
> the final score).

> **Ring-40 upgrade (la soudure)**: that merge is no longer manual —
> `python3 lab/fw40-merge.py chain dump.rom` runs exam + identify +
> weld + score as ONE command, with four machine-checked laws: schema
> (only oracle keys), separation (day-0 and release-41 keys never
> mix), precedence (identify deep-truth beats the exam shallow walk —
> proven live: the shallow walk picks the wrong DSDT variant and the
> nested-LZMA armor booleans; the judge wins and the conflict is
> journaled), provenance (every value cites exam|identify|lens). The
> remaining MANUAL lenses (IFR façade, varstore share, PSP hashes)
> enter via `--lenses manual.json` afterwards and the command re-welds.
> The dump's scoreline is read against the registered envelope
> (`lab/vendor-scorelines.json`): known rungs score 10/8/7/4 hits by
> chronological distance, a foreign image scores exactly the stranger
> signature {P-04, P-05, P-18} — a 3644-neighbour should land at the
> 3604 end of the envelope with only P-18 missing. Release-41 uses
> `chain-pair release40.rom release41.rom` (the null-model from the
> historical 3802→3810 pair is registered beside it).

> **Ring-41 upgrade (the third reading)**: predicted and known no longer
> exhaust the dump — `python3 lab/fw41-novelty.py novelty dump.rom`
> answers the third question: what lies OUTSIDE every register and every
> prediction envelope. Six axes (species, certs, DSDT, AGESA, whitelist,
> SMM) each compare the image to a re-derived universe; a verdict of
> `inside-universe` on a known rung is the coverage proof, `novel` on the
> dump means entries for the zero ledger
> (`lab/vendor-novelty-ledger.json` — empty until an event surprises the
> registers, each entry carrying axis/value/class provenance). The
> `anticipated` class keeps the P-02 envelope ({1.2.0.6, 6a, 6b, 6c, 7,
> 8}) and the P-20 floor (1.2.0.12, within the 1.2.0.x line) from
> false-novelling — the detector and the oracle interlock without
> duplicating verdicts. Run it AFTER `chain`, merge the three readings
> (predicted | known | novel) into §1-§7 below.

> **Ring-42 upgrade (le dossier — the three readings become ONE
> command)**: the merge above is no longer manual —
> `python3 lab/fw42-dossier.py dossier dump.rom` welds the three
> readings (oracle scoreline + identity verdict + novelty scan) into ONE
> document and confronts them with each other. Every non-aligned
> scoreline row gets a COUPLING: `confirmed` (the detector's entries
> explain the miss — candidate zero-ledger material), `bare` (no
> detector entry — suspect the lenses or a deeper divergence), `blind`
> (one of the 9 predictions the detector cannot see — the manual lenses
> are the only witness), `identity` (P-18). The identity × detector
> pairing gets its own verdict: `known-clean` (the dump IS that
> release), `coherent-foreign` (a stranger the detector can name — the
> OVMF/TUF profile), `suspicious` (the judge sees, the detector is
> silent — check the blind axes first), or a REFUSAL (byte-identity +
> unregistered content = instrument disagreement; fix the registers
> before trusting anything). Read the final PROFILE against the
> register `lab/vendor-dossier-register.json`: all nine known rungs
> score clean (the monotone decline 10→8→7→6→5→4→4→4→4 hits is the
> measured context), a stranger scores {P-04, P-05, P-18} + named
> residue, and a 3644-neighbour should land at the 3604 end with P-18
> hit and the rest of the coupling table explaining every deviation.
> Release-41 uses `dossier-pair old.rom new.rom` — a non-empty
> `brand_new_species` list is the beyond-register event, and those
> GUIDs seed the zero ledger. Fill the ten manual lenses exactly as
> before (`--lenses manual.json` re-welds); the dossier command accepts
> everything `chain` accepted.

> **Ring-43 upgrade (les propositions — the seven pastes become seven
> reviews)**: the manual lens slots are no longer pasted by hand.
> `python3 lab/fw43-proposal.py propose dump.rom --out proposals.json`
> machine-drafts all seven LENS_ONLY slots — the three PSP body-hash
> SETs (the dump itself fills `psp_hashes_3644`; the on-disk 3604/3802
> rungs fill the comparison slots with the same calibrated method) and
> the IFR quartet (`ifr_questions_total`, `ifr_valid_fraction`,
> `setup_uncovered_bytes`, `varstore_uncovered_share_pct`) — each with
> its provenance printed, each calibrated against the frozen registers
> (`vendor-proposal-calibration.json`: 47 rows, 45 agree, 0 disagree;
> any disagreement = the group refuses to propose, the slot stays
> manual). A slot the proposer cannot fill comes back OMITTED with its
> reason — never a guess. The sidecar `proposals.json.report.json`
> carries the provenance and omissions for the record. Then review:
> confirm or CORRECT each value, and pass the file as
> `--lenses proposals.json` to `dossier` — corrections beat proposals,
> always (fw40's precedence law journals the override). The
> judge-owned slots (matches_known_release, distinct_fingerprints_count,
> trust_cert_distinct) are NEVER proposed — fw39 fills those as before.

3644 is absent from three official ASUS ledgers and the ring-11
"3644 zip" was proven a 240-byte HTML page (`vendor-ledger-tuf.json`).

- H1 (bad acquisition): closed — the HTML page was the "3644" file.
- H2 (ledger omission): tested across PRIME 39, TUF ATX 33, TUF
  mATX 33 — empty.
- H3 (the board ships something the ledgers do not list — OEM/
  factory build): **the dump decides**. If §1 triangulates to a
  release whose version string reads 3644 or nearby, record: version
  string, AGESA level, PSP fingerprint set, NVRAM defaults — and
  re-check the ledgers with those exact values.

> **Ring-44 upgrade (the corrected board — question_3644 resolved by
> acquisition)**: the founder corrected the board BEFORE the dump —
> it is a **TUF GAMING B550-PLUS WIFI II** (TG550PW2.CAP), not the
> B450-PLUS class the §2 frame assumed. The question above is CLOSED
> without the dump: 3644 IS published — in the WIFI II ledger
> (2026/08/27, AGESA ComboV2 PI 1.2.0.12, fTPM CVE-2026-6726/6727)
> and the non-WIFI sibling's; absent from WI-FI v1 (quiet since
> 3636). H2 proven, literal-H3 refuted; the machine-H3 class
> (`no-match-h3-eligible`) is nonetheless CONFIRMED on the acquired
> 3644 — the corpus was B450, the board never was. Consequences for
> the day-0 protocol, all pre-registered in
> `lab/vendor-ledger-b550w2.json`:
>
> - **Expected profile is now MEASURED, not hypothetical**: the
>   acquired b550w2-3644.rom (sha16 5f7a0e439772c160, rom =
>   cap[0x1000:], 32 MiB) scored **4 hit {P-03, P-06, P-08, P-18} /
>   8 miss / 14 na**, profile `stranger`, coupling
>   `coherent-foreign`. A stock board on 16/09 should reproduce
>   EXACTLY this scoreline with identity resolving `known-clean`
>   against the acquisition (full-file or body convention). Every
>   deviation is the board's own NVRAM/flash story — read it through
>   the dossier coupling table as designed.
> - The oracle register stays frozen: the 8 misses are the MEASURED
>   board-dependent facts (32 MiB geometry, AGESA 1.2.0.12, armor
>   present, 0 legacy SMM, DSDT bdd18b8af25e0c7a, 6 certs, SMM 186) —
>   the transfer map {4 board-independent invariants survive a
>   B450→B550 move} is the ring's product, not a register edit.
> - **A second command lands beside `dossier`**: byte-compare the
>   dump against the acquisition. `cmp` at the byte level settles
>   stock-vs-modified in one step; the 3645 cartography (53 runs,
>   81 KB uncompressed-code delta) is the registered map for the
>   post-update world ("un meilleur bios" = flash 3645, then
>   `dossier-pair` it against the dump — the release-41 machinery,
>   live today, one day early).
> - The 3644-vs-3645 and WIFI-II-vs-sibling maps already live in
>   `differ-b550w2-3644-3645.json` (session) and the register: a
>   version number never identifies an image (the two 3644s differ by
>   32.0% of bytes).

> **Ring-45 upgrade (the B550 lens layer — the seven slots get their
> anchors)**: ring 44 read the lens slots as loud-degraded nulls and
> attributed the nulls to calibration non-transfer; ring 45 measured
> the attribution — **the proposers never refused**: `fw43.propose`
> runs on the corrected board unchanged (7/7 slots, 0 omitted, IFR
> validity 1.00); the nulls were this file's protocol not passing
> `--lenses`. The corrected board now has its own L2 basis
> (`lab/vendor-b550-lens.json`): 57 anchor rows on the three
> acquisitions + 10 shared B450 rows, re-derived before any proposal
> (law L2-transfer). Consequences for the day-0 protocol:
>
> - **The lens slots are proposed, anchored, and reviewed against the
>   anchors**: `python3 lab/fw45-b550-lens.py propose dump.rom --out
>   proposals.json` marks each slot `== stock-3644 anchor` or `!=`
>   with the anchor value beside it; operator review; then
>   `dossier dump.rom --lenses proposals.json` (7/7 land as journaled
>   lenses — proven live).
> - **Doubly pre-registered scorelines** (`vendor-b550-lens.json`
>   lens_witnessed_scoreline): stock board WITHOUT lenses = the
>   measured 4 hit {P-03, P-06, P-08, P-18} / 8 miss / 14 na; WITH
>   lenses = **6 hit {P-03, P-06, P-08, P-13, P-16, P-18} / 1 partial
>   (P-15: 161 inside its registered widened [110,174]) / 10 miss /
>   9 na** — machine-derived through the frozen oracle, not hand
>   counted. P-19 misses either way once lenses land: the cross-family
>   PSP overlaps measure 40 == 40 and the checker's strict inequality
>   fails on equality.
> - **The flash-state discriminator**: the IFR quartet is IDENTICAL
>   between 3644 and 3645 (release identity law) — it cannot tell
>   stock from flashed-3645; the PSP set can (37-body delta). The
>   `!=` marker on `psp_hashes_3644` plus the set difference against
>   both registered sets reads the flash state directly.
> - The 74-vs-60 novelty accounting delta is RECONCILED
>   (`lab/vendor-novelty-recon.json`): a key-naming asymmetry
>   (`extra` vs `unregistered`), fw41 unchanged, the 14 = the WIFI
>   II's AUX-fan/Level vocabulary, zero new chip families (missing =
>   0). Consumers of the serialized novelty axes read the whitelist
>   `extra` key — or re-derive.
> - The exam's `setup_varstore_size` stays null on B550 (the NVRAM
>   variable row) and that is a DIFFERENT quantity from the IFR
>   cross's Setup varstore size 515 — P-14 stays na; the conflation
>   is pre-empted in the register.

> **Ring-46 upgrade (the auto-dossier — the WHOLE protocol is now ONE
> command)**: rings 43/45 removed the paste and gave the corrected
> board its anchors, but the protocol was still THREE commands and one
> memory. `lab/fw46-autodossier.py` fuses them —
> **`python3 lab/fw46-autodossier.py fused dump.rom --out
> dossier.json`** runs propose (L2-transfer gates first), marks every
> slot against the stock-3644 anchor, applies your corrections
> (L1: corrections beat proposals, journaled), and lands the full
> triptych dossier — the whole provenance chain in ONE artifact:
>
> - **The review moment is cheap and stays yours**: `--review-only`
>   stops after propose + marks (no dossier); pass your corrected
>   values back as `--lenses corrections.json` on the fused run —
>   corrections beat proposals, always, and the journal says which
>   was which.
> - **The marks are SET-anchored now** (the ring's measured lesson):
>   the ring-45 marks compared PSP sets by COUNT, and 3645 has exactly
>   203 hashes too (the 37-body swap is INSIDE the set) — a flashed
>   board would have read `==` everywhere. fw46 carries the registered
>   sets (203/194/193, `vendor-autodossier-register.json`): on a
>   flashed-3645 board `psp_hashes_3644` marks `!=` with added 37 /
>   removed 37 while the IFR quartet stays `==` — the discriminator,
>   visible in the review table where it matters.
> - **The with-lenses scoreline is MEASURED, not just derived**: the
>   crown fused run on b550w2-3644.rom landed 6 hit {P-03, P-06,
>   P-08, P-13, P-16, P-18} / 1 partial {P-15} / 10 miss / 9 na —
>   exactly the ring-45 derivation — with the stranger point
>   (no-match-h3-eligible / coherent-foreign / stranger / 74
>   unregistered). Expected on a stock board: marks all `==`, that
>   scoreline, that identity; any deviation is the board's own
>   NVRAM/flash story, and the PSP set mark reads the flash state
>   directly.
>
> **Ring-47 upgrade (the pair witness — the post-flash world gets its
> set-level reading)**: ring 46 healed the count-blindness in the
> day-0 chain, but the release-41 machinery (fw42 `dossier-pair`) still
> read the pair through SPECIES only — and the measured release
> 3644→3645 kills zero species: it would have read clean-pair, the
> NULL profile, on the discriminator pair itself. The morning after a
> flash, run:
>
> - **`python3 lab/fw47-pairmarks.py marks dump.rom
>   b550w2-3645.rom`** — the cheap set-level review: on a flashed-3645
>   board the PSP set marks `!=` with added 37 / removed 37 (anchor
>   checked against the lens register) while the count witness reads
>   `==` (203 vs 203) and species read 0 births / 0 deaths — the
>   corrected class is **swap-event**, and any deviation from THOSE
>   marks is the board's own NVRAM/flash story, read structurally.
> - **`python3 lab/fw47-pairmarks.py cohere b550w2-3644.rom
>   b550w2-3645.rom`** — the full corrected pair dossier (fw42
>   composed, never rewritten): the frozen clean-pair confronted to
>   the corrected class, the **profile_correction NAMED** in the same
>   artifact. The crown measured it in 206 s: 18 modules changed at
>   −992 B module-size drift against the differ's 7,544,486 B
>   run-level — content swaps inside stable containers, now a visible
>   event class.
> - **The board signature** (measured, for contrast): the SAME version
>   number across boards (3644 → nw-3644) kills **4 species** (108
>   modules, −13,008 B, PSP 65/65) — corrected class
>   registered-births. The version-number trap now has a structural
>   pair-level signature: a release swaps content (swap-event), a
>   board change kills species (registered-births).
> - **The null model corrected**: the ring-42 frozen pair-null
>   (3802→3810, clean-pair) re-read through the set lens was itself
>   count-blind — 17/17 PSP hashes inside 193-hash sets, 34 modules,
>   −86,264 B. Verdict recorded in `vendor-pairmarks-register.json`;
>   the frozen register stands untouched.

> **Ring-48 upgrade (the full-axis pair witness — the dump reads on
> EVERY axis)**: ring 47 witnessed the PSP axis at pair level; the
> release's other content axes were pair-blind. fw48 extends the set
> lens to the five non-PSP content axes (certs / dsdt / whitelist /
> smm / agesa), composes on fw47 unchanged, and derives each pair's
> SIGNATURE from the full evidence. Measured on three pairs (the
> crown `vendor-pairaxes-register.json`): the release 3644→3645
> rotates ONE DER certificate (`2da09da7f4131f9f` →
> `4aa03fa22cd75c84`) inside a stable count of 6 — the blindness
> class on a new axis — while the DSDT, whitelist, SMM and AGESA sets
> are exactly identical; the same-version sibling swaps ALL FOUR DSDT
> variants (zero overlap, the WIFI II's `bdd18b8af25e0c7a` is
> SKU-exclusive) while the trust set transfers verbatim; the B450
> 3810 release rebuilds its trust store 4→6 (the corpus's freeze
> point, pair-explained). THE AXIS MAP: **certs is a RELEASE marker,
> dsdt is a BOARD marker** — orthogonal signatures, both hidden from
> every count-level mark. Pre-registration ledger: 8 PRs, 5 hit /
> 3 refuted, all visible (the cert rotation, the B550-generation fan
> vocabulary, no measured pair instantiates psp-localized-swap).
> Day-0 additions to the arsenal:
>
> - **`python3 lab/fw48-pairaxes.py axes dump.rom`** — the cheap
>   per-image reading: the live axis sets (counts + items + meta).
> - **`python3 lab/fw48-pairaxes.py marks dump.rom
>   b550w2-3644.rom`** — the stock story: every content axis expected
>   `==` (the dump's NVRAM does not reach these sets); any `!=` is a
>   lead, not a verdict.
> - **`python3 lab/fw48-pairaxes.py marks dump.rom
>   b550w2-3645.rom`** — the flash story: psp 37/37 AND the cert
>   rotation are the expected swap marks; any OTHER axis moving is
>   the board's own NVRAM/flash story.
> - **The dump law**: the cert set alone discriminates stock-3644
>   from flashed-3645 (the rotation items are registered,
>   count-blind-proof), and the DSDT set alone certifies the board
>   identity — two new discriminators beside the PSP set.

> **Ring-49 upgrade (the ReBAR axis — the founder's "reshade bar",
> measured)**: the founder reported "le reshade bar ... il détecte mal
> alors que c'est en auto" — Resizable BAR on Auto, detected badly —
> and the repo's diag-gpu already renders the OS half (gpu-bar1-small:
> BAR1 <= 256 MiB) while stating the BIOS half is "not observable from
> the OS". Ring 49 measures that BIOS half on the vendor images
> (`lab/fw49-rebar.py`, crown `vendor-rebar-register.json`): the
> option EXISTS on this board — the ASUS label is **"Resize BAR
> Support"** (the word "Resizable" is absent from the whole decompressed
> image, which is why canonical searches find nothing), ONE_OF qid
> 0x20F at Setup-varstore offset 0x1BB, options {0x00, 0x01}, wrapped
> in SUPPRESS_IF(Above4gDecode==0) and GRAY_OUT_IF(qid 0x361==1), and
> the vendor's own help text documents the chain verbatim: 64-bit PCI
> decoding + **CSM disabled in the Boot section** + a ReBAR-capable
> GPU. **The release 3644→3645 is rebar-axis-INVARIANT** (same
> construct hash): the 16/09 flash changes nothing here — the stock
> reading below stands for the flashed board too. The board pair is
> invariant modulo exactly one byte (the grayout operand's qid
> 0x361→0x35F on the sibling). Day-0 additions:
>
> - **`python3 lab/fw49-rebar.py axis dump.rom`** — the full truth
>   chain on the live dump (L2-anchored: every qid/tok/voff must
>   reproduce the crown register or the instrument refuses loud).
> - **The ReBAR reading**: expect the axis identical to stock-3644 on
>   the dumped image; any divergence is a lead, not a verdict.
> - **The live checklist (day-0/day-1, omarchy)**: if the machine
>   "detects badly" with the option on Auto — check the three vendor
>   conditions: Above 4G Decoding enabled, **CSM disabled (Boot
>   section)**, GPU vBIOS ReBAR-capable; then `omarchy-firmware
>   diag-gpu` (gpu-bar1-small fires when ReBAR is off), `sudo lspci
>   -vv -s <gpu>` for the "Physical Resizable BAR" current size,
>   `dmesg | grep -iE 'rebar|resizable'`, and GPU BARs above the 4 GB
>   line in /proc/iomem.

> **Ring-50 upgrade (the ReBAR language — what the options actually
> say)**: ring 49 froze the ReBAR AXIS; ring 50 decodes its LANGUAGE
> (`lab/fw50-rebarlang.py`, crown `vendor-rebarlang-register.json`).
> The Setup module's strings are ONE standard HII package (header
> @367586: Length 0x041DC2, Type 0x04, LanguageWindow all-zero, tag
> "en-US") whose id-less blocks obey **tok = ordinal + 1** — verified
> by the forward map hitting the tok-1281 anchor in EXACTLY 1280 steps.
> THE VOCABULARY, measured: tok 3 "Enabled", tok 4 "Disabled", tok 5
> "Auto" — Above4gDecode/SriovSupport/CsmSupport are {Disabled,
> Enabled}, and **ResizeBarSupport is {0x00: Disabled, 0x01: Auto} with
> NO Enabled option: "c'est en auto" is byte-true and Auto is the ONLY
> path to engagement**. The grayout operand is NAMED: qid 0x361 = a
> hidden NUMERIC u8 at varstore 4 offset 0 = **SystemAccess** (1 byte,
> GUID E770BB69-…) — the option grays out for restricted setup
> sessions. Two ring-49 readings refuted, kept visible: the toks 3/4/5
> "SCSU" reading (plain UTF-16 — the failure was ring 49's own backward
> walker: 50 mis-steps/1280 measured) and the "WIFI questions" reading
> (the extra qids read **SecureVarPresent[0]/[4]**, Secure Boot
> variable-presence). The language is INVARIANT under the 16/09 flash.
> Day-0 additions:
>
> - **The five-gate Auto checklist (in order)**: (1) Above 4G Decoding
>   Enabled — else the ReBAR question is SUPPRESSED entirely; (2) Resize
>   BAR Support = Auto (the founder's state, byte-confirmed; there is no
>   Enabled to force); (3) CSM disabled in the Boot section; (4) a
>   ReBAR-capable GPU; (5) an unrestricted setup session
>   (SystemAccess[0] == 1 grays the option out — enter with
>   administrator rights).
> - **`python3 lab/fw50-rebarlang.py lang dump.rom`** — the language
>   reading on the live dump (L2-anchored to the ring-49 crown; any
>   refusal is a lead, not a verdict).
> - **`python3 lab/fw50-rebarlang.py nulllang`** — the ring-49 walker
>   flaw as a permanent measurement (the honest index is the forward
>   map).

> **Ring-51 upgrade (the factory law of the ReBAR chain — what the
> vendor actually ships)**: ring 49 measured the axis, ring 50 the
> language; ring 51 (`lab/fw51-rebarfact.py`, crown
> `vendor-rebarfact-register.json`) reads what the vendor SHIPS — the
> acquisitions' NVAR stores first-parsed (two windows
> @0x40078/0x1040078; the outer store holds ONE variable, StdDefaults;
> the nested store walks clean, 17 entries; the inner Setup blob 515 B
> matches the ring-50 IFR census). THE FACTORY LAW, two sources
> crossed (StdDefaults canon + IFR DEFAULT statements, 15/15 agree):
> **the five gates ship OFF — ReBAR 0x00 Disabled @0x1BB, Above 4G
> 0x00 @0x1BA, CSM 0x00 @0x1F2, Sriov 0x00, Mmio 0x27 — on all three
> acquisitions, release- and board-invariant, and the B450 3604 null
> ships the SAME disabled chain (the law is generation-wide)**. With
> Above 4G shipped off, the SUPPRESS_IF hides the ReBAR question at
> factory: "c'est en auto" is a WRITTEN state on the founder's board,
> not the factory state. Day-0 additions:
>
> - **Factory identity**: the dump's StdDefaults blob must reproduce
>   sha16 `c127f5cb3e69cbb5` — a different sha is a flash-history
>   marker (fw48's marks first).
> - **The five-gate byte court** (`python3 lab/fw51-rebarfact.py day0`
>   prints the card; `fact dump.rom` reads both sides): the dump's
>   outer store WILL carry live variables (the CAP ships none) — read
>   the live Setup blob at 0x1B6/0x1BA/0x1BB/0x1BC/0x1F2 and delta
>   against the factory canon per gate. Expectations REVERSED by ring
>   51: live Above4g expected **0x01** (the founder SEES the question),
>   live ReBAR expected **0x01** (he reads Auto), live CSM expected
>   0x00. A live byte breaking expectation IS the measured story
>   (above4g-drift / rebar-drift / csm-drift).
> - **Live SystemAccess** (1 B, GUID E770BB69-…): the factory ships
>   none; a live 0x01 means the option is GRAYED on the board — a
>   restricted-session story, not a detection story.
> - **The verdicts**: all-five-faithful at the engaged values ⇒ the
>   firmware chain is byte-faithful and the symptom lives in the Auto
>   heuristic (vendor code — the named open question) or the OS half
>   (the §2 ReBAR half above).

> **Ring-52 upgrade (the grammar of the Setup variable — what the
> vendor's own C source says the bytes ARE)**: rings 49–51 addressed
> the Setup variable through three lenses (IFR, language, factory);
> ring 52 (`lab/fw52-rebargram.py`, crown
> `vendor-rebargram-register.json`) adds the vendor's OWN vocabulary:
> the image ships the AMI SETUP_DATA structure as raw C-source text
> (GUID AB017B39-…, 53,050 B) — 396 named fields, packed 515 B ==
> the measured blob, and every gate lands EXACTLY on the ring-49
> crown voffs as NAMED fields (MmioAddrLimit 0x1B6, Above4gDecode
> 0x1BA, **ResizeBarSupport 0x1BB**, SriovSupport 0x1BC, CsmSupport
> 0x1F2) — the third independent source. The executor census: 34
> consumers of the Setup GUID (set a0da64f48529c2a3), exactly two ≥
> 1 MB — the 4.25 MB policy bridge (release-invariant
> cbf56bec7d3cfaac, B550-generation) and the Setup module itself. The
> board pair's single grammar delta is the field `MyAsusControl`
> (@0x201 — ring 51's anonymous tail span [513,514], NAMED). Day-0
> additions:
>
> - **Grammar identity**: the dump's SETUP_DATA text must reproduce
>   sha16 `9bfea5a1540da7f1` (packed 515) — flash-immune
>   (release-invariant); a different sha is an acquisition-mismatch
>   marker before anything else is read.
> - **Executor identity**: bridge sha16 `cbf56bec7d3cfaac`, 34
>   consumers, set `a0da64f48529c2a3`, name-table `c3af43f04b68d805`
>   — equally flash-immune; `python3 lab/fw52-rebargram.py day0`
>   prints the card, `executor dump.rom` reads the dump's side.
> - **The delta speaks FIELD NAMES**: the §2 five-gate byte court's
>   live-minus-factory protocol (ring 51) now names its bytes from
>   the image's own C source — above4g-drift, rebar-drift, csm-drift
>   are `SETUP_DATA` fields, not anonymous offsets.
> - **Scope note**: the Auto heuristic's internal logic stays
>   vendor-code territory — bounded (behind 34 readers, inside one
>   4.25 MB release-invariant binary), named, never read. The
>   symptom's three candidate homes are unchanged (§2 Ring-51): the
>   heuristic, the vendor help's conditions, or the OS half.

## 3. Delta vs 4655 (ring-15 vocabulary)

Run the release-delta lens with the dump in the "old" slot.

| class | count | % of set |
|---|---|---|
| unchanged | | |
| changed | | |
| changed · same-size | | |
| changed · resized | | |
| added | | |
| removed | | |
| **byte-identical share** | | |

### 3.1 Flash-armor checklist (ring-16 bisect — versioned expectations; ring-20 generalization)

Generate the row set from `vendor-flash-armor.json` at run time; do
not transcribe GUIDs by hand. Shape of the table:

| module | GUID | expected if ≥3802 (2022-05) | expected if ≥4604 (2024-04) | present in dump? |
|---|---|---|---|---|
| PrepareWhiteListSmm / SbRomArmorSmm / FlashSmiSmm / FlashSmiDxe (+2 unnamed) | from register | 5 of 7 from 3802 | all 7 | |
| legacy SMM pair | from register | both present if ≤4003 (2023-03); 1 of 2 at 4202 | both gone by 4402 | |

Reading: armor presence dates the build independently of any string
— the second wave completed at 4604, so 7/7 present means "4604 or
newer", 5/7 means "3802–4402 era", 0/7 means pre-3802 (2022-05).
Bound it independently with the AGESA rungs (§1.1): the two signals
disagreeing is itself a finding.

### 3.2 Byte-span forensics (ring-16 vocabulary)

For the top same-size pairs: changed bytes, span count, prefix16
equality. Watch for the ring-16 species (single-digit surgical
patches vs wholesale replacements at 0 % overlap).

## 4. The chain run (the generale, ~8 s)

Six lenses in protocol order, two runs, registers byte-compared
(`vendor-day0-chain.json` is the rehearsal to match):

| lens | exit | seconds | register sha256_16 |
|---|---|---|---|
| unwrap | | | |
| census | | | |
| versions | | | |
| psp | | | |
| nvar | | | |
| depex | | | |

Chain verdict: [ ] REPRODUCIBLE (both runs identical) [ ]
COMPLETE-NOT-IDENTICAL (record which register drifted and the
species — the ring-10/15 set-iteration class is the known offender).

## 5. Optional seventh lens — the combo/windows lens (ring 17)

Runs only if the dump is 32 MiB or shows paired top-level FVs at
+half-image. Procedure: locate `PSP2`/`BHD2` dwords (bytes on disk
spell `2PSP`/`2BHD`), parse the 32-byte header + 16-byte entries per
`combo_directory.h` (doc #55758), validate fletcher32, resolve every
`lvl2_addr` under the three address conventions, list the per-family
`$PSP`/`$BHD` directories. Expected on a 16-MiB ASUS dump: zero
combo tables — the absence is itself the row.

| table | offset | count | fletcher32 | targets |
|---|---|---|---|---|
| | | | | |

## 6. Names, phases, DEPEX

| signal | measured | rehearsal value (ASUS 4655) |
|---|---|---|
| modules total | | 600 |
| named by UI section | | low (ASUS ships ONE UI section) |
| named by 532-GUID join | | 0.593 overall, DXE 0.83 / SMM 0.86 |
| DEPEX parsed | | 286/286 |
| apriori / BEFORE-AFTER count | | (the ring-8 rule: the count itself is a finding) |

## 7. Honesty notes for the day

- Body-hash identity, never header bits — the "one-byte change"
  lesson is institutional.
- Names come from the join; never invented. A GUID with no name is
  recorded as a GUID — on a vendor image that means new silicon,
  itself a finding.
- Every claim cites its register; every register exists before the
  narrative that quotes it.
- The +0-octet rule is verified in §0 and stands for the whole
  session. No SPI write, no flash, no daemon, no ESP/NVRAM touch.
