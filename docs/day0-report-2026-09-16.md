# Day-0 report — ASUS PRIME B450-PLUS live dump (2026-09-16)

Status: **template**. Every table is filled on the day, from the
measured registers, with the frozen instruments. Nothing here is a
prediction; expectations are cited only so the measurement can agree
or disagree with them. Fill order follows `docs/day0-protocol.md`.
The +0-octet rule governs everything: the dump lands on the offline
drive, the SPI is never written, no flash, no daemon — the update
decision belongs to the official ASUS tools, and this report only
sharpens it.

## 0. Acquisition record

| field | value |
|---|---|
| date/time (UTC) | |
| board | ASUS PRIME B450-PLUS |
| programmer / tool | |
| dump size | |
| sha256 (full) | |
| +0 octet check (SPI writes) | |
| host OS | |

Sanity gate before anything else: the dump size decides which lenses
run. 16 MiB → single-window path (all six lenses). 32 MiB or paired
structures → also run the combo lens (see §5).

## 1. Identity triangulation — three independent paths

Fill each path *before* looking at the others; disagreement between
paths is itself a finding.

### 1.1 AGESA level (ring-16 lens)

| signal | measured | nearest known |
|---|---|---|
| `ComboAM4v2PI` string | | nine dated rungs on this board (ring 18): 1.2.0.6b=3604 · 1.2.0.7=3802/3810 · 1.2.0.8=4003 · 1.2.0.A=4202 · 1.2.0.B=4402 · 1.2.0.Ca=4604 · 1.2.0.E=4631 · 1.2.0.12=4655 — a level between two rungs dates the build between their releases |
| pierced-surface count | | raw scan is not enough — the strings sit 2.7–2.9 MiB deep; ladder lives in `vendor-agesa.json` (timeline) |

### 1.2 PSP fingerprints (ring-13 lens)

| dir | magic | count | fletcher32 | sha256_16 | nearest known release |
|---|---|---|---|---|---|
| | | | | | |
| | | | | | |

Compare against `vendor-psp.json` (the five known specimens). The
ring-13 cross-release register (37 identical blobs, 38 changed
between 3604 and 4655) is the dictionary.

### 1.3 NVRAM factory fingerprint (ring-14/16 lens)

| signal | measured |
|---|---|
| NVAR anchor store size | (3604 and 4655 both measure 130,952 B — size alone decides nothing) |
| anchor content sha | |
| HWM/QFan region vs 4655 | (ring 16: their 778-B diff sits in `NV_SIO0_LD1` / `SetupHWMOneof` / `QFan` spans) |
| `Setup` blob size + sha | (factory: 456 B on ASUS) |
| shared variable names | (expect the 9-name canon: AMITSESetup, NetworkStackVar, PCI_COMMON, PlatformLang, SecureBootSetup, Setup, Timeout, UsbSupport, XhciDID) |

### 1.4 Identity verdict

| box | meaning |
|---|---|
| [ ] | equals 4655 (2026-08-27) |
| [ ] | equals 3604 (2022-03-16) |
| [ ] | equals another known release (name: ) |
| [ ] | matches no known release — H3 survives, the 3644 question reopens with three new inputs (§2) |

### 1.5 Ring-19 fingerprints — the fourth signals (fill alongside §1.1-1.3)

Ring 19 added independent first-hour signals measured on the ten
on-disk ASUS rungs; all expect a clean match here.

| signal | measured | expectation from the registers |
|---|---|---|
| Authenticode DER set (raw scan) | | SIGHTINGS, not distinct certs: 18 expected (12 if pre-3810) = each trust certificate shipped 3× across the PK/KEK/db store copies; DISTINCT bodies: 4 if pre-3810, 6 from 3810 (the register's "12→18" collapses — `vendor-der-inventory.json`). Set = ASUS leaf (Private Organization TW 23638777) + DigiCert G4 Code Signing 2021 CA1 + the rotating timestamp chain; the timestamp responder DATES the build: 2021=3604 · 2022-2=3802/3810 · 2023=4402/4604 · 2024+leaf-renewal=4631 · 2025 responder=4645/4655; leaf validity window 2024-05-27→2027-05-30 on a 4631+ build is itself a dating read |
| geometry (64 KiB entropy atlas) | | FV share 41-42 % of 16 MiB, packed 56-60 %, erased 18-19 %, top 0xFF slack 1.05-1.09 MiB, NVAR magic 18-19 (`vendor-geometry.json`); deviation = pre-parse anomaly flag. **Instrument: `lab/fw36-atlas.py atlas` computes this fingerprint in seconds on the dump** (ring 36). Two ring-36 boundaries: raw diffs of compressed regions measure the LZMA container, not the content (the 3-byte NX truth amplifies 322,734x raw on OVMF) — read SECFV raw for a build-difference signal, module-walk FVMAIN for semantics; and NVAR is AMI-specific (census 0 on OVMF's auth-variable store) |
| armor anatomy (for §3.1) | | `SbRomArmorSmm` one byte-identical body since 3802; `PrepareWhiteListSmm` carries `AMD rom armor` + SPI chip DB (ADESTO/AMIC/ATMEL...), 3 bodies across rungs; SMM census expected 107-109 (`vendor-lifecycles.json`) |
| facade magnitude (for §6) | | ~7,975-8,273 questions / 779-783 pages across ~15 modules on this board; ~zero SUPPRESS/GRAYOUT/DISABLE conditions; 100 % exact-consumption validity expected (`vendor-ifr-census.json`) |
| five-clock decision tree (ring 20) | | the dating clocks fuse cost-ordered: (1) geometry — seconds, raw read, expect FV 41-42 % / packed 56-60 % / slack ~1.06 MiB, deviation = anomaly flag; (2) cert set — raw scan, no pierce: 12 DERs = pre-3810, 18 = 3810+, timestamp year dates the build — the ONLY clock separating 3802 from 3810; (3) AGESA strings — pins the rung except the quiet pair; (4) armor census — 0/7 pre-3802, 5/7 from 3802, 7/7 from 4604, legacy 2/2 if ≤ 4003; (5) PSP churn vs neighbours. `(cert_count, AGESA)` alone resolves all nine rungs (`vendor-clocks.json`) |
| microcode clock (ring 21) | | psptool `0x66` scan — raw directory read, seconds, no pierce: expect EXACTLY 19 distinct patches (the carrier constant; any other count is a finding), newest patch date 2024-08-22 for a 4631+/1.2.0.E-era build (older year-buckets map at `vendor-microcode.json`); the 19-patch set corroborates the rung alongside `(cert_count, AGESA)` — the quiet pair 3802|3810 is NOT split by this clock either |
| DSDT clock (ring 21) | | checksum-admitted DSDT body hash against the three-generation atlas: `27d5e826` (3604-4202, 47,184 B) / `f23c571b` (4402-4604, 45,043 B) / `0a4a6f16` (4631-4655, 45,069 B); creator INTL 2014-09-25 expected on every AML body; a FOURTH body hash is a finding (`vendor-acpi.json`) |
| SSDT set clock (ring 22) | | the eighth clock, independent of the DSDT's frontiers: expect the 35-body AML atlas (4 DSDT variants + 23 RAW-SEC SSDTs + 8 EMBEDDED ALIBs) with the SSDT SET matching the three-set atlas (3604 | 3802-3810 | 4003-4655) and the 6 ALIB bodies byte-identical corpus-wide — any deviation is a finding (`vendor-ssdt-clock.json`) |
| armor chip whitelist (ring 22) | | the `AMD rom armor` banner's chip-name table: expect the SAME 46-family set byte-for-byte (frozen 3802→4655, identical on all five vendors); a different family count or set is a corpus first — 3604-era builds carry the 41-family predecessor instead (`vendor-armor-chipdb.json`) |
| ACPI shipped-vs-built (ring 21) | | expect DSDT variants + SSDTs shipped as checksum-valid raw sections inside the compressed FV, and ZERO FACP/APIC/MCFG/HPET/IVRS bytes anywhere in the image (built at runtime by the AMI ACPI layer) — finding any static-table byte would be a corpus first (`vendor-acpi.json`) |
| Setup-blob unasked bytes (ring 20) | | the `Setup` varstore on this board: 456 B declared, ~142 B never referenced by any question (83.4 % of all varstore bytes unasked), expected as 27 merged ranges (largest 28 B @ +49) at offset level (`vendor-hidden-map.json`); IFR-side size must match the NVRAM-side blob (ring 14) — a mismatch is a finding (`vendor-unasked.json`) |
| module roster + word clock (ring 23) | | two first-hour reads, zero pierce needed beyond the standard FV/FFS walk: (a) MODULES — expect ~613 FFS files with the armor quintet present (`755877A6`/`6C289241`/`391626DB`/`51080191` + freeform `89BE47F4`, born 3802) and freeform `02076249` + `4EB43107` (born 4604), the legacy pair `827E45A4`/`21782819` ABSENT on any ≥4402 build (`vendor-churn-atlas.json`); (b) WORDS — the HII string layer decodes to ~5K distinct strings and MUST contain `PSP RPMC Switch` (born 3802, the armor release's only new words) and, on a 4631+-lineage build, `Stability Boost` + `PCI Express Native Power Management` (`vendor-vocabulary-clock.json`) — any of these absent/present at the wrong rung is a finding |

## 2. The 3644 decision tree

> **Ring-35 upgrade (the oracle)**: this dump now runs against a frozen
> register of 19 quantitative expectations (`lab/vendor-oracle.json`,
> target `day0-3644`, scored by `lab/fw35-oracle.py score`). Fill
> `fw35-oracle.py template` alongside §1-§3 — the lenses below produce
> exactly the observed values the register wants — then score: every
> hit confirms a law, every partial/miss localizes which law failed to
> extend, and each requires a written cause in the next ring report.
> Registered highlights: AGESA ∈ {1.2.0.6b, 1.2.0.6c, 1.2.0.7} (point
> 1.2.0.6c), armor quartet ABSENT, 2 legacy SMM modules present, DSDT
> `27d5e826e111d755`, 4 trust certs, facade 7,900-8,050 questions,
> Setup varstore 456 B with 142 never-asked, SMM census 106-109, and
> identity = H3 with ≥ 3 independent fingerprint differences (P-18).

> **Ring-37 upgrade (the differ, tracked)**: the §3 module walk, byte
> pins and store read are now one tracked instrument —
> `lab/fw37-differ.py` (16-gate two-tier selftest, tier I live on the
> OVMF corpus: the ring-34 anchors re-derived to the byte). Day-0 use:
> `fw37-differ.py scan dump.rom` (first-hour inventory, UI names with
> no census), `fw37-differ.py ledger 3644.rom dump.rom` (the GUID-
> aligned ledger — the P-18 fingerprint generator),
> `fw37-differ.py pin ...` (byte-exact localization inside changed
> modules), `fw37-differ.py vars dump.rom` (the store lens). The same
> command path serves release-41 (`ledger release40.rom release41.rom`
> = the P-20..P-26 measurement input).

> **Ring-38 upgrade (l'examen blanc)**: the whole chain is now ONE
> command — `python3 lab/fw38-exam.py exam dump.rom --out
> findings-draft.json` runs geometry (fw36), inventory + pierce +
> vars (fw37), the AGESA ladder, armor names/GUIDs, SMM census, legacy
> SMM, the checksum-validated ACPI walk, the PSP candidate scan, the
> chip-whitelist match, assembles the fw35 findings draft (12 of the
> 20 day-0 fields auto-filled; the 10 manual lenses stay null and are
> LABELED), and scores the draft. The dress rehearsal (`fw38-exam.py
> mock`) proved on the OVMF stand-in that the oracle DISCRIMINATES:
> the only day-0 hits on a non-target image are the absence-predictions
> P-04/P-05, release-41 hits are ∅, and the rebuild wall re-derives at
> genome level (18 births / 9 deaths / +777,472 B). Ring-36 correction:
> its DSDT "hit" @2259668 was a length-plausibility false positive —
> the new walk validates checksums and refuses audibly (OVMF has NO
> static DSDT: tables are runtime-generated via QemuFwCfgAcpiPlatform).

> **Ring-39 upgrade (le moteur d'identité)**: the H3 question gets its
> judge — `python3 lab/fw39-identity.py identify dump.rom` compares the
> dump against ALL nine registered releases on eight scored axes
> (rom-sha under both measured conventions, AGESA, the four DSDT
> variants, SMM census, trust certs, armor by GUID, genome profile) and
> returns a verdict wired to P-18: `known-release` (byte-identity),
> `no-match-h3-eligible` (≥3 independent fingerprints from EVERY known
> release — the H3 shape), or `no-match-below-floor` (1-2 — a repack or
> neighbour). Calibrated live on the corpus: all nine releases
> self-identify at 0 diffs, TUF 4645 no-matches at 3, and the separation
> floor between known rungs is 1 semantic axis — so clearing 3 means
> MORE distinct than any pair in the chronology. It also fills
> `trust_cert_distinct` (P-11) and carries deep-walk overrides for the
> armor/legacy/whitelist lenses (the armor wave lives behind a nested
> LZMA section — merge the identify output into the exam draft before
> the final score).

> **Ring-40 upgrade (la soudure)**: that merge is no longer manual —
> `python3 lab/fw40-merge.py chain dump.rom` runs exam + identify +
> weld + score as ONE command, with four machine-checked laws: schema
> (only oracle keys), separation (day-0 and release-41 keys never
> mix), precedence (identify deep-truth beats the exam shallow walk —
> proven live: the shallow walk picks the wrong DSDT variant and the
> nested-LZMA armor booleans; the judge wins and the conflict is
> journaled), provenance (every value cites exam|identify|lens). The
> remaining MANUAL lenses (IFR façade, varstore share, PSP hashes)
> enter via `--lenses manual.json` afterwards and the command re-welds.
> The dump's scoreline is read against the registered envelope
> (`lab/vendor-scorelines.json`): known rungs score 10/8/7/4 hits by
> chronological distance, a foreign image scores exactly the stranger
> signature {P-04, P-05, P-18} — a 3644-neighbour should land at the
> 3604 end of the envelope with only P-18 missing. Release-41 uses
> `chain-pair release40.rom release41.rom` (the null-model from the
> historical 3802→3810 pair is registered beside it).

3644 is absent from three official ASUS ledgers and the ring-11
"3644 zip" was proven a 240-byte HTML page (`vendor-ledger-tuf.json`).

- H1 (bad acquisition): closed — the HTML page was the "3644" file.
- H2 (ledger omission): tested across PRIME 39, TUF ATX 33, TUF
  mATX 33 — empty.
- H3 (the board ships something the ledgers do not list — OEM/
  factory build): **the dump decides**. If §1 triangulates to a
  release whose version string reads 3644 or nearby, record: version
  string, AGESA level, PSP fingerprint set, NVRAM defaults — and
  re-check the ledgers with those exact values.

## 3. Delta vs 4655 (ring-15 vocabulary)

Run the release-delta lens with the dump in the "old" slot.

| class | count | % of set |
|---|---|---|
| unchanged | | |
| changed | | |
| changed · same-size | | |
| changed · resized | | |
| added | | |
| removed | | |
| **byte-identical share** | | |

### 3.1 Flash-armor checklist (ring-16 bisect — versioned expectations; ring-20 generalization)

Generate the row set from `vendor-flash-armor.json` at run time; do
not transcribe GUIDs by hand. Shape of the table:

| module | GUID | expected if ≥3802 (2022-05) | expected if ≥4604 (2024-04) | present in dump? |
|---|---|---|---|---|
| PrepareWhiteListSmm / SbRomArmorSmm / FlashSmiSmm / FlashSmiDxe (+2 unnamed) | from register | 5 of 7 from 3802 | all 7 | |
| legacy SMM pair | from register | both present if ≤4003 (2023-03); 1 of 2 at 4202 | both gone by 4402 | |

Reading: armor presence dates the build independently of any string
— the second wave completed at 4604, so 7/7 present means "4604 or
newer", 5/7 means "3802–4402 era", 0/7 means pre-3802 (2022-05).
Bound it independently with the AGESA rungs (§1.1): the two signals
disagreeing is itself a finding.

### 3.2 Byte-span forensics (ring-16 vocabulary)

For the top same-size pairs: changed bytes, span count, prefix16
equality. Watch for the ring-16 species (single-digit surgical
patches vs wholesale replacements at 0 % overlap).

## 4. The chain run (the generale, ~8 s)

Six lenses in protocol order, two runs, registers byte-compared
(`vendor-day0-chain.json` is the rehearsal to match):

| lens | exit | seconds | register sha256_16 |
|---|---|---|---|
| unwrap | | | |
| census | | | |
| versions | | | |
| psp | | | |
| nvar | | | |
| depex | | | |

Chain verdict: [ ] REPRODUCIBLE (both runs identical) [ ]
COMPLETE-NOT-IDENTICAL (record which register drifted and the
species — the ring-10/15 set-iteration class is the known offender).

## 5. Optional seventh lens — the combo/windows lens (ring 17)

Runs only if the dump is 32 MiB or shows paired top-level FVs at
+half-image. Procedure: locate `PSP2`/`BHD2` dwords (bytes on disk
spell `2PSP`/`2BHD`), parse the 32-byte header + 16-byte entries per
`combo_directory.h` (doc #55758), validate fletcher32, resolve every
`lvl2_addr` under the three address conventions, list the per-family
`$PSP`/`$BHD` directories. Expected on a 16-MiB ASUS dump: zero
combo tables — the absence is itself the row.

| table | offset | count | fletcher32 | targets |
|---|---|---|---|---|
| | | | | |

## 6. Names, phases, DEPEX

| signal | measured | rehearsal value (ASUS 4655) |
|---|---|---|
| modules total | | 600 |
| named by UI section | | low (ASUS ships ONE UI section) |
| named by 532-GUID join | | 0.593 overall, DXE 0.83 / SMM 0.86 |
| DEPEX parsed | | 286/286 |
| apriori / BEFORE-AFTER count | | (the ring-8 rule: the count itself is a finding) |

## 7. Honesty notes for the day

- Body-hash identity, never header bits — the "one-byte change"
  lesson is institutional.
- Names come from the join; never invented. A GUID with no name is
  recorded as a GUID — on a vendor image that means new silicon,
  itself a finding.
- Every claim cites its register; every register exists before the
  narrative that quotes it.
- The +0-octet rule is verified in §0 and stands for the whole
  session. No SPI write, no flash, no daemon, no ESP/NVRAM touch.
